# zero-core 环境变量示例
#
# 用法:复制为 .env(已 gitignore)按需填入。所有变量可选 —— 不设走默认。
# 仅列代码实际读取的变量;provider API key 不走 env(在应用内 Settings / 配置文件配置)。

# ─── 数据目录 ───────────────────────────────────────────────
# zero-core 数据根(db/core.db、wiki、skills、attachments 等)。
# 默认: ~/.zero-core
# ZERO_CORE_DIR=/path/to/.zero-core

# ─── 调试 ───────────────────────────────────────────────────
# =1 打开 debug 级别日志(文件 + 控制台)。
# ZERO_CORE_DEBUG=1

# ─── 代理(企业防火墙/Brave Search/provider 走代理)──────────
# Node fetch 默认不读系统代理;这里显式开启。
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# ─── 后端端口 ───────────────────────────────────────────────
# 后端 Express 端口。Electron 模式下主进程用 --port=0 随机分配,本变量仅 CLI/
# 独立运行时生效。默认 3210。
# PORT=3210

# ─── Node 标准 ──────────────────────────────────────────────
# production / development。
# NODE_ENV=development

# ─── 仅测试用(普通使用勿设)─────────────────────────────────
# =1 进入测试 fixture 模式(静态 test-seed)。E2E 专用。
# ZERO_CORE_TEST_FIXTURE=1
