# styles

## 核心功能
全局样式定义，包含应用的基础 CSS 样式和 CSS 变量定义。

## 输入
themes/ 目录下的主题覆盖样式

## 输出
全局 CSS 样式和 CSS 变量（供所有 renderer 组件消费）

## 定位
src/renderer/styles/ — 渲染进程样式层，定义全局视觉基础

## 依赖
./themes/（主题覆盖样式）；供所有 renderer 组件使用

## 维护规则
- 全局样式变更需检查对所有页面的影响
- 新增 CSS 变量需在此文件中定义默认值
- 组件级样式优先在组件内或 themes 中处理
