# 一夜文档循环 · 追踪文件

> 跨触发记忆。每次全新 session 读这里接着干。只有这个文件 + charter 是状态来源。

## Vision
把 zero-core 的 docs/ 做得更清晰、更详实、结构更好、更多可视化/可交互。允许打破现有结构。
独立分支 docs/overnight-20260625,明早人审后合并。

## 规则摘要
- 只动 docs/。每次一个连贯改进就 commit 收工。
- 不 push/merge/动源码/跑构建。
- commit 带 Co-Authored-By: Claude <noreply@anthropic.com>。

## 候选 backlog(初始种子,按价值排序,可改)
- [ ] 核对 docs/arch/02-module-structure.md 与 src/ 实际模块树,补/改过时路径
- [x] 核对 docs/basic/file-structure.md 与当前目录布局 —— 见 #9(全文重写,所有计数按实际 src/ 树核对:17→25 工具 / 10→13 组件目录 / 10→15 store / 4→7 hook / 5→6 mcp-tools / 删"16 handler 模块"叙事 / 补 v0.8 工作流域 9 store 分组 + main 4 文件 + subagent-delegation 死代码标注)
- [x] docs/arch/03-runtime-engine.md:补厚 AgentLoop / hook 机制(PreLLMCall/PostTurnComplete),加 mermaid 序列图 —— 见 #1
- [x] docs/arch/04-tools-subsystem.md:工具注册/路由/权限模型 + 25 个工具 v0.8 矩阵 + buildTool 横切关注点(hook+遥测+限流)+ Agent vs AgentRegistry 区分 —— 见 #5
- [x] docs/arch/05-persistence.md:SqliteStore 通用 CRUD + 各 Store + migration,加 ER 图(mermaid) —— 见 #7(原表只覆盖 11 张业务表,ER 图漏了 v0.8 9 张工作流域表 + duplicate 块;本次补 §2.2b 新表矩阵 + 拆 §2.13 为 2.13a/2.13b 双 ER 图;§3/§10/§11 同步)
- [x] docs/arch/06-knowledge-subsystems.md:wiki 目录镜像树 + archivist 增量扫描 + 摘要懒加载(v0.8 本次刚改),务必同步 —— 见 #2
- [x] docs/arch/07-renderer-and-ipc.md:data-change-hub + data-sync + IPC ROUTE_MAP 派生 + non-2xx reject —— 见 #3
- [ ] docs/visualization/code-graph-data.json 与 src/ 当前结构对齐(27k 行,需谨慎,优先核对顶层节点而非全量重生成)
- [x] docs/arch/08-cross-cutting.md:后端子进程生命周期 / 全局错误兜底(v0.8 本次刚加) —— 见 #4
- [ ] 新增 docs/visualization/ 下可交互页:数据流(data-change-hub 推送)、wiki 懒加载树演示
- [x] docs/arch/02-module-structure.md §2.1 边界约束提到 "test-seed.ts 有 type-only import" —— 核对是否仍准确 —— 见 #6(实际还有 `fresh-db-seed` 值导入,但生产路径门控不可达)
- [x] **[new, #8]** 05 §4.0.3 新叙事("v0.8 工作流域 store 在 server/index.ts 独立 new,SessionDB 只当 getDb() 提供者")应反向同步到 02-module-structure.md —— 见 #10(02 §4 / §4.1 / §4.1.1 / §4.4 / §8.4 全改:文件规模表对齐 v0.8 行数 + 重写"11 个 SqliteStore"为 A/B/C 三类 + 新增 §4.1.1 编排边界 + 启动顺序 10 步化 + renderer store 10→14+1)
- [ ] **[new, #10]** 03-runtime-engine.md 若提到 SessionDB / store 依赖注入 —— 核查是否也停留在 v0.7 "SessionDB 是聚合根"叙事(grep 已确认 03 当前**零** "SessionDB"/"聚合根" 命中,可能是真没问题,但下次扫 03 时顺手核对 store 相关段落是否需要补 v0.8 编排边界链接)
- [x] docs/arch/03-runtime-engine.md §1 表 "subagent-delegation.ts(326) 已废" —— 核对 v0.8 Agent action 工具落地后该文件是否还在 src/ 实际被 import —— 见 #6(确认零 importer,死代码;但委派实走 `subagent-delegator.ts` 的 SubagentDelegator 类,不是"统一走 Agent 工具"那么简单)
- [x] docs/arch/07-renderer-and-ipc.md §3.7 mermaid 拓扑图仍是 v0.7 store 集合 —— 下次可补 project/requirement/wiki/cron/notification store 节点 + data:changed 边(本次只改文字,未动图) —— 见 #11(已重画为双路径图:StreamEvent→AppLayout + data:changed→各 store 自订阅;补 5 个 v0.8 store 节点 + 4 个工作流域页面 + 删过时 AgentToolStore + 修 ChatStore 字段计数 11→8)
- [ ] docs/arch/04-tools-subsystem.md 或 08-cross-cutting.md:核对 orchestrate-handlers.ts / pm-handlers.ts 的 main 进程单例(ConfirmRegistry / PmService)为什么必须留主进程而不能下放 REST(07 §2.5 ① 提到但未展开)—— **08 §12.1 已部分回答(ABI 边界),但 ConfirmRegistry/PmService 单例本身未展开,留作下次**
- [ ] docs/arch/08-cross-cutting.md §12.3 自愈无退避策略 + §12.5 uncaughtException 不 exit —— 已在 §12.7 评价里标 open question,下次可补"重启计数 + 通知主进程弹窗"的具体设计提案(若用户认可)
- [x] docs/arch/08-cross-cutting.md §2.4 "30 个事件 / 23 个未装载" 计数来自旧版,#1 已在 02/03 修正 hook 清单 —— 08 §2.4 的具体事件表还是旧的(本次只改了 §2.2/§2.3/§2.5/§2.6,没动 §2.4 表),下次可把 §2.4 表与 02 §3.2 实际注册清单对齐 —— 见 #14(§2.4 表逐行核对:30 事件按"实际触发 / 仅定义"二分,20 个真活跃 + 10 个零触发幽灵;§2.6 评价同步从"23 个未实现"改为"10 个零触发";每个事件标 file:line 触发位置)
- [ ] **[new, #6]** `code-graph.html`(内联 27k 行 DATA)与 `code-graph-data.json` 都把 `subagent-delegation.ts` 与 `subagent-delegator.ts` 作为合法节点列出 —— 与 #6 在 arch md 里建立的"前者是死代码"叙事不一致。理想做法:在 `code-graph-data.json` 给 `subagent-delegation.ts` 加 `"dead": true` / `"supersededBy": "subagent-delegator.ts"` 元数据,code-graph 渲染时灰显并标删除候选。下次触发可考虑(27k 行需谨慎,只改这两个节点不要全量重生成)。
- [ ] **[new, #6]** `code-graph.html` 内联 DATA 里 `test-setup.ts` 标 lines=31 且 imports=[],但实际它 re-export 了 `test-seed.ts` 的值(`isTestMode`/`seedTestEnvironment`/`TestSeedResult`)—— 内联数据 imports 缺这条边。下次做 code-graph-data.json 对齐时一并修。
- [x] **[new, #7]** 05 §4.2 "迁移机制" 行号还写 `db-migration.ts:91-223`,但 v0.8 后该函数已扩到 1059 行(91-897+) —— 见 #8(已重写为 5 阶段:列补齐 / v0.8 表 DDL / SqliteStore 构造 / JSON→SQLite / KV+Memory,正确行号 `runMigrations` 在 584-1004)
- [x] **[new, #7]** 05 §4 提到 SessionDB 持有 KeyValueStore/MemoryStore/MemoryNodeStore,但 v0.8 后 SessionDB 还应该持有(或被注入)ProjectStore/RequirementStore/CronStore/WikiStore 等 —— 核对 session-db.ts 实际是否聚合这些 store,还是各自独立 new —— 见 #8(**核对结果**:SessionDB **不**聚合工作流域 store —— 只有 3 个 eager 内核 store + 2 个 v0.8 M5 lazy store(ExtractionCursorStore/TelemetryStore);9 个工作流域 store 在 `server/index.ts:148-171` 独立 new,把 SessionDB 当 `getDb()` 提供者用)
- [ ] **[new, #7]** 06 §2 已记 project_wiki 磁盘镜像树,但 05 §2.2 旧表里 kb_entries/kb_chunks 与 project_wiki 的"三套 wiki 并存"关系(§11.2 新提)在 06 没有专门小节对比 —— 可在 06 加一节"三套知识系统对比矩阵"(kb_*/memory_nodes/project_wiki 各自的写入触发器 / 检索方式 / 适用场景)。
- [ ] **[new, #8]** 05 §4.0.3 新叙事("v0.8 工作流域 store 在 server/index.ts 独立 new,SessionDB 只当 getDb() 提供者")应反向同步到 02-module-structure.md(它对 SessionDB / store 的分层描述可能还停留在 v0.7"SessionDB 是聚合根"叙事)和 03-runtime-engine.md(若那里提到 store 依赖注入)。下次做 02 核对时一并查这两篇。
- [ ] **[new, #8]** 05 §4.0.2 表里 ExtractionCursorStore/TelemetryStore 标 lazy,但 07-renderer-and-ipc.md §3.2 的 store 列表若也提这两个,应同步标"lazy + 不进 db-migration"的注释。优先级低,纯交叉引用对齐。
- [ ] **[new, #9]** `docs/basic/` 其他几篇(app-flow.md / backend-structure.md / tech-stack.md)可能也有同类 stale 计数(#9 只修了 file-structure.md)。下次可统一扫一遍 basic/ 把过时数字 / 路径修对(优先级低,本系列定位是速查表)。
- [ ] **[new, #9]** file-structure.md §"server/ 详细结构" 的 store 分组用了域(domain)切分,但 [`02-module-structure.md`](../arch/02-module-structure.md) 的 server 章节可能还是按文件名字母序或老 v0.7 分组 —— 若用户认可 domain 切分更清晰,可把 02 的 server 部分也改成同构分组(避免维护者要在两套分组间脑补映射)。下次做 02 核对时一并考虑。**注:#10 已部分处理** —— 02 §4.1 改成 A/B/C 三类(按归属方式 = 是否挂 SessionDB + v0.8 阶段),与 file-structure.md 的"域"切分(会话核心/旧业务/工作流域/路由层/服务编排)不完全同构,但是两种合理的正交切分(归属 vs 域),下次若统一可考虑用归属维度为一级、域为二级。
- [x] **[new, #10]** 05 §4.0.2 标题写"6 个,分两批",但表格只列 5 个 store(3 eager + 2 lazy),正文 §4.0.2 第 529 行又把 WikiStore 算作"+1 个全局 wiki store 入口"凑成 6 —— 实际 WikiStore 在 `server/index.ts:122` 独立 new,**不在 SessionDB 内**。标题应改为"5 个(3 eager + 2 lazy)",正文那句"+1 个全局 wiki store 入口"是错的(混淆了 index.ts:122 的 WikiStore 与 SessionDB 内聚合)。下次触发可修(改 05 一个文件即可,小改)。 —— 见 #12
- [x] **[new, #10]** 02 §8 components/ 目录树(11 个目录)与 #9 的 file-structure.md(13 个目录)计数不一致 —— file-structure.md 是 13,02 是 11,差 2 个可能是 v0.8 新增的 project/requirement/wiki/cron 组件目录或 dashboard/workspace 的差异。下次触发核对 `ls src/renderer/components/` 实际并对齐 02 §8 列表。 —— 见 #13(02 §8.2/§8.3 全改:11→13 目录、删幽灵 `workspace/`、补 cron/requirements/wiki 3 个 v0.8 目录、agents section 计数 5→6 + 重列、settings section 计数 9→7、common 补 NotificationToast、dashboard 删"待规划")

## Progress log(最新在上)
<!-- 每次触发追加一行: #N | <time> | <做了什么> | files | <commit> -->
- #14 | 2026-06-27 02:25 | 把 08-cross-cutting.md §2.4 事件分类表与 src/ 实际 `triggerHooks` / `HookRegistry.trigger` 调用逐行核对(backlog #32,跨章节一致性修复,#1 早就修了 02/03 的 hook 清单但 08 §2.4 的事件表一直没动)。**问题背景**:旧 §2.4 表是 v0.7 视角的"按类别分组 + 笼统触发位置",**计数和触发位置都不准**:① 表里 `Setup` 当成 Session 生命周期事件列在"agent-loop.run / .finalizeStream" —— 实际 `grep "Setup" src` 在 `hook-types.ts:31` 定义之外**零命中**(没有任何 triggerHooks 调用);② `TaskCreated` / `TaskCompleted` 标"未装载" —— 实际 `subagent-delegator.ts` 在 `delegateTask` / `delegateTaskBackground` / `runBackground` / `stopTask` 路径里大量触发(199/212/218/269/270/307/319/339/360/373/397/404 共 12 个触发点),这些是 v0.8 委派重构后的**活跃事件**;③ `Notification` 写"触发位置 = TaskRegistry" —— 实际触发点是 `notification-hooks.ts:60`(`HookRegistry.getInstance().trigger("Notification", ...)`),TaskRegistry 只是数据源;④ `TeammateIdle` 标"未装载"是对的,但没说清它和 `TaskCreated`/`TaskCompleted` 的区别(后者是活跃的委派任务事件,前者是占位);⑤ "装载状态" 块写 "✅ 活跃 8 个 / ⚠️ 未装载 23 个" —— **既漏算**(实际活跃 20 个),又把"未装载"等同于"未触发"(真正的幽灵事件只有 10 个);⑥ 8+23=31 与总数 30 都对不齐。**传染点**:§2.6 "可以改进的" 第一条也写 "23 个 hook 定义但未实现" 并举例 `PermissionRequest` / `Elicitation` / `ConfigChange` —— 但 `Elicitation` 实际活跃(AskUser 工具 `tools/ask-user.ts:56,70` 触发),举例本身就错。改:① **§2.4 标题** 加"(类型定义见 `core/hook-types.ts:29-39`)";② **整张表重写** 从"按类别分组 + 笼统触发位置"改为**30 行逐事件矩阵**:每行明确"是否实际触发"(✅/⚠️)+ "实际触发位置(file:line,多个触发点全列)";活跃事件全部标到具体行号(SessionStart=agent-loop.ts:236,296;StopFailure=agent-loop.ts:466;PreToolUse=tool-factory.ts:181 + agent-loop.ts:678;SubagentStop=subagent-delegator.ts 8 个触发点;TaskCreated/TaskCompleted 各 4-6 个触发点;Elicitation/ElicitationResult=ask-user.ts:56,70;Notification=notification-hooks.ts:60 标"不经 triggerHooks 包装";PreCompact/PostCompact=session.ts:156,220,229);幽灵事件(`Setup`/`TeammateIdle`/`PermissionRequest`/`PermissionDenied`/`ConfigChange`/`CwdChanged`/`FileChanged`/`WorktreeCreate`/`WorktreeRemove`/`InstructionsLoaded`)标 "⚠️ 仅定义,零触发";③ **装载状态块** 重写为正确计数:**20 个活跃**(列全)+ **10 个零触发**(列全,解释是参考 Claude Code 27 事件点提前占位的扩展点);④ **新增"过去版本的描述错在哪"更正块**:显式列出旧"活跃 8 / 未装载 23"的两个错误(漏算 12 个本活跃的事件 + 把"未装载"等同"未触发"),让维护者看到旧版判断为何不准;⑤ **新增同名陷阱提示**:`TaskCreated`/`TaskCompleted` 是活跃委派事件,**不是**旧文档暗示的"TeammateIdle 同类未实现占位";⑥ **§2.6 评价** 第一条从"23 个 hook 定义但未实现"改为"10 个 hook 在类型里定义但代码零触发",并把错举例(`Elicitation`)替换为正确的 10 个幽灵事件全列。源码逐行核对:`core/hook-types.ts:29-39`(HookEventName 联合 30 个事件名)/ `grep -oE 'triggerHooks\(\s*"[A-Za-z]+"' src`(提取 19 个 distinct 事件名,Notification 不在内)/ `grep -oE 'HookRegistry.getInstance\(\).trigger\(\s*"[A-Za-z]+"' src`(单命中 `Notification` at `notification-hooks.ts:60`)/ 30 - 20 = 10 个零触发事件名核对(Setup/TeammateIdle/PermissionRequest/PermissionDenied/ConfigChange/CwdChanged/FileChanged/WorktreeCreate/WorktreeRemove/InstructionsLoaded)/ `agent-loop.ts:228,236,251,268,269,296,312,313,466,497,678,705,726,766,798` / `session.ts:156,220,229` / `tool-factory.ts:181,206,220` / `subagent-delegator.ts:140,177,187,199,211,212,217,218,228,269,270,306,307,318,319,338,339,360,373,397,404` / `ask-user.ts:56,70` / `notification-hooks.ts:60` / `durable-hooks.ts:42,60,75,89`(SessionStart/PostToolUse/Stop/StopFailure 4 个 durable handler 确认) / `tool-execution-hooks.ts:38,49,84`(PreToolUse/PostToolUse/PostToolUseFailure 3 个 tool-exec handler 确认) / 02 §3.2 已是 7 个 runtime + 2 个 server handler(无须改动) | docs/arch/08-cross-cutting.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #13 | 2026-06-27 01:50 | 对齐 02 §8.2/§8.3 渲染层组件目录与 07 §5.2/§5.4 页面 section 计数(backlog #43,跨文件一致性修复)。**问题背景**:02 §8.3 列了 11 个 components/ 子目录,但 `ls src/renderer/components/` 实际 13 个 —— 漏了 v0.8 工作流域的 `cron/` / `requirements/` / `wiki/` 3 个目录,且**误列了一个不存在的幽灵目录 `workspace/`**(实际 components/ 下没有 workspace/;01-system-overview.md:257 与 05-persistence.md:18 出现的 `workspace/` 是仓库根的默认工作区目录,是另一回事,不动);`dashboard/` 标"待规划中"但实际已有 `DashboardPage.tsx`。同时 02 §8.3 内文 stale:`agents/` 写"5 个 section"但 AgentEditor 实际 6 个(Basic/Prompt/Permissions/Subagents/Tools/WikiAnchors),且没列全 TemplateGallery/TemplateCard/TemplateDetailModal/GithubImportModal/agent-editor-types.ts;`settings/` 写"9 个 Section"但实际 SettingsPage + 7 个 section 文件(Theme/Proxy/Memory/Guidelines/DeviceContext/Workspace + Provider Card/Editor);`common/` 漏 NotificationToast.tsx。**传染点**:07-renderer-and-ipc.md §5.2 也有同样的"5 个 section + ExposeAsTool"错列(实际无 ExposeAsTool section,有 Subagents+WikiAnchors),§5.4 也写"9 个 section"且列表自身重复(Theme 和 ThemeSettings、DeviceContext 和 DeviceContextSettings 同时列)。file-structure.md #9 已是正确的 13 目录,本次不动它。改:① **02 §8.2** 加一句注:`AppLayout.tsx` 也在 layout/ 下,layout/ 实际 6 个文件(原描述只列 5 个面板,把 AppLayout 隐去);② **02 §8.3 重写目录树**:开篇明示 13 个子目录 + 解释 v0.8 新增 4 个(cron/requirements/skills/wiki)+ 显式说明删 `workspace/` 的理由(避免读者误以为它存在);逐目录补真实文件清单(agents 列 6 个 section + 4 个 template/modal + types;common 补 NotificationToast;cron 单文件 CronDashboard;requirements 列 8 个文件含 Kanban/CoverageJudgement;wiki 列 WikiPage/WikiTree/WikiDetail 链 06;settings 改"8 个文件 = SettingsPage + 7 个 section"列全);③ **07 §5.2** AgentEditor section 计数 5→6 + 列表改正(Basic/Prompt/Tools/Permissions/Subagents/WikiAnchors)+ 链 02 §8.3;④ **07 §5.4** SettingsPage 9→7 个 section + 列表去重 + 链 02 §8.3。源码逐行核对:`ls src/renderer/components/`(13 目录:agents/chat/common/cron/dashboard/kb/layout/mcp/requirements/settings/skills/tools/wiki,**无 workspace**) / `ls src/renderer/components/agents/`(13 文件:AgentsPage/AgentEditor + BasicSection/PromptSection/ToolsSection/PermissionsSection/SubagentsSection/WikiAnchorsSection + TemplateGallery/TemplateCard/TemplateDetailModal/GithubImportModal + agent-editor-types.ts) / `AgentEditor.tsx:33-38`(import 6 个 Section 确认) / `ls src/renderer/components/settings/`(8 文件:SettingsPage + DeviceContext/Guidelines/Memory/Proxy/Theme/Workspace + ProviderCard/ProviderEditor) / `ls src/renderer/components/requirements/`(8 文件) / `ls src/renderer/components/{skills,tools,dashboard}/`(各单文件) / `ls src/renderer/components/common/`(5 文件含 NotificationToast) / grep `workspace/` 确认仅 01:257 / 05:18 / rfc 三处指根目录非 components | docs/arch/02-module-structure.md, docs/arch/07-renderer-and-ipc.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #12 | 2026-06-27 01:25 | 修 05 §4.0.2 + §4.0.3 + §11.2 + file-structure.md 里"SessionDB 聚合 6 个 store"的连锁事实错误(backlog #42,#10 触发时记下的最小改)。**问题背景**:① §4.0.2 标题写"SessionDB 直接聚合的 store(**6 个**,分两批)",正文写"3 个 eager + 2 个 lazy store **+ 1 个全局 wiki store 入口**",但**表格只列 5 行**(3 eager + 2 lazy),第 6 个根本不存在 —— 这是把 `server/index.ts:122` 的 `wikiStoreGlobal = new WikiStore(sessionDB)` **误算进 SessionDB 聚合集**;实际 `WikiStore`(`wiki-node-store.ts:327`)在 `session-db.ts` 里 grep 零命中 —— SessionDB **不**持有 WikiStore 字段、不暴露 getter,只被当 `getDb()` 提供者传进 WikiStore 构造函数。② 这个错计数**传染**到了同文件 §4.0.3 第 561 行("DB 句柄 + 5 张自持表 + **6 个内核 store** 的提供者")和 §11.2 第 810 行("拆分压力只剩会话核心 5 张表 + **6 个内核 store**");③ 还传染到了 `docs/basic/file-structure.md:108`("SessionDB 自持,5 张表 + **6 个聚合 store**")。02-module-structure.md 不受影响(#10 已正确改成 "5 个聚合 store(eager 3 + lazy 2)" + §4.1.1 A 类表)。改:① **§4.0.2 标题** "6 个,分两批" → "5 个,分两批";**正文**删去 "+ 1 个全局 wiki store 入口" 错误尾巴;**新增"⚠️ 更正"块** 显式说明 WikiStore 在 server/index.ts:122 独立 new(链 §4.0.3 + 02 §4.1.1),解释它实例化早(hooks 注册前)与"是否在 SessionDB 内"是两件事;② **§4.0.3 第 561 行** "6 个内核 store" → "5 个内核 store" + 一句"以及 §4.0.2 更正块提到的 WikiStore"把它当 getDb() 提供者;③ **§11.2 第 810 行** "6 个内核 store" → "5 个内核 store";④ **file-structure.md:108** "6 个聚合 store" → "5 个聚合 store" + 顺手把"会话核心聚合根"改成正确的"会话核心 + DB 句柄提供者"(与 §4.0.3 否定"聚合根"叙事一致,链 05 §4.0.2 更正块 / §4.0.3)。源码逐行核对:`grep "WikiStore" src/server/session-db.ts`(零命中,确认 SessionDB 不聚合 WikiStore) / `grep "new WikiStore" src/server`(单命中 `index.ts:122: const wikiStoreGlobal = new WikiStore(sessionDB)`) / `session-db.ts:50-105`(构造函数 + 6 个 getter 实际确认:kvStore/memoryStore/memoryNodeStore eager + extractionCursorStore/telemetryStore lazy,**无 WikiStore 字段**) / `wiki-node-store.ts:327`(export class WikiStore 确认) / 02 §4.1.1 已正确(无须改动) | docs/arch/05-persistence.md, docs/basic/file-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #11 | 2026-06-27 00:50 | 重画 07-renderer-and-ipc.md §3.7 Zustand Store 拓扑 mermaid 图(种子 backlog 第 #29 项,跨章节一致性修复)。**问题背景**:旧 §3.7 图停留在 v0.7:① 只画了一条 `StreamEvent → AppLayout → store` 边,**完全漏了 v0.8 关键的 `data:changed` 第二条事件路径**(各 store 自己用 `data-sync.ts` 订阅,不经 AppLayout);② 漏了 v0.8 工作流域全部 5 个 store(project / requirement / wiki / cron / notification);③ 列了已不存在的 `AgentToolStore` 节点(v0.7 残留,v0.8 工具配置已下沉到服务端 `tool_configs` 表 + `agents.tools` JSON 列,前端工具列表由 `AgentStore.fetchTools()` 从 `agents/{id}/tools` 拉来);④ ChatStore 标"11 字段"过时(实际 8);⑤ 漏了 v0.8 新增的工作流域页面节点(RequirementsPage / WikiPage / CronPage / DashboardPage);⑥ Backend WS 节点没区分两条反向通道(StreamEvent vs data:changed)的语义差异。改:① **顶部加 v0.8 更正块**,显式列出两条事件路径的分工 —— StreamEvent 集中路由 chat 流 + 工作流通知(只更新 chat / interaction / notification / requirement 4 store),data:changed 由各 store **自订阅** 5 个 collection(`agents` / `projects` / `requirements` / `crons` / `project_wiki`),与 AppLayout 完全无关;② **重画图**:把 WS 反向通道拆成 StreamEvent + data:changed 两节点,后者用虚线箭头连到 5 个工作流域 store(显式标"各 store 自订阅,模块副作用"),保留 AppLayout 作为 StreamEvent 唯一订阅者;③ **补 5 个 v0.8 store 节点**(ProjectStore / RequirementStore / WikiStore / CronStore / NotificationStore)+ 4 个工作流域页面节点(DashboardPage / RequirementsPage / WikiPage / CronPage),用浅蓝/浅绿配色与旧 store/页面区分;④ **删过时 AgentToolStore 节点**;⑤ **修正 ChatStore 字段计数** 11→8 + 在"关键观察"列出 8 个实际字段名;⑥ **新增"两条事件路径并行,互不替代"** 关键观察(讲清 StreamEvent 推"事件+增量字段",data:changed 推"完整记录"的本质差异);⑦ **新增"WikiStore 是唯一用 subscribeDataChange(全量 refetch)而非 subscribeListDataChange(增量 patch)的 store"** 观察 —— 因为 wiki 是树形结构,链回 §2.3.1 实际订阅矩阵;⑧ **新增"AppLayout 收到 requirement_notification 时顺手调一次 RequirementStore.fetchRequirements() 是冗余刷新"** 观察(因为 data:changed 也会推 requirements collection)。源码逐行核对:`ls src/renderer/store/`(14 store + data-sync = 15 文件) / `grep "subscribeDataChange\|subscribeListDataChange" src/renderer/store/`(5 处订阅:agent-store:136 / cron-store:127 / project-store:90 / requirement-store:184 / wiki-store:198,确认 wiki 用 subscribeDataChange 其余用 subscribeListDataChange) / `AppLayout.tsx:92-194`(handlers 完整表 14 类事件 + 4 store 更新点:ChatStore / InteractionStore / NotificationStore / RequirementStore) / `AppLayout.tsx:166-175`(requirement_notification handler 确实顺手调 fetchRequirements) / `AppLayout.tsx:196-202`(onAgentEvent 单一订阅确认) / `chat-store.ts:151-158`(8 个 state 字段确认) / `ls src/renderer/components/`(13 目录含 v0.8 新增 cron/requirements/skills/wiki) | docs/arch/07-renderer-and-ipc.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #10 | 2026-06-27 00:25 | 反向同步 05 §4.0.3 的 "SessionDB 不是聚合根" v0.8 叙事到 02-module-structure.md(backlog #36,跨文件一致性修复)。**问题背景**:#8 已在 05 重写 §4 + 新增 §4.0.3 论证 SessionDB **不**聚合 9 个 v0.8 工作流域 store(它们在 `server/index.ts:148-171` 独立 new),但 02 §4 仍停留在 v0.7 "SessionDB = 所有 store 的聚合门面" 叙事 —— ① §4 重头戏表行数过时(wiki-node-store 1090→1698 / agent-service 1010→1200 / db-migration 930→1060 / session-db 850→960 / index.ts 700→904);② §4.1 "11 个 SqliteStore 包装类" 列表完全没覆盖 v0.8 工作流域 9 store + 还列了已删的 agent-tool-store.ts(v0.8 §11.5 退役);③ §4.1 把 session-db.ts 描述为"聚合门面" 暗示它是所有 store 的父聚合根,与 05 §4.0.3 矛盾;④ §4.4 "启动顺序固定: SessionDB → runMigrations → ToolRegistry → Stores → ..." 线性序列漏了 v0.8 关键约束(WikiStore 必须早于 hooks 注册,否则 M5 抽取器拿不到 writer);⑤ §8.4 "10 个 store" 是 v0.7 计数(v0.8 加了 project/requirement/wiki/cron + notification = 14)。改:① **§4 重头戏表** 全部对齐实际行数 + 补 v0.8 角色(db-migration 标 5 阶段链 05 §4.2 / wiki-node-store 标 v0.8 磁盘镜像树链 06 §2.5 / index.ts 标 "手动编排 12+ store" / session-db 标 "会话核心 5 表 + 5 聚合 store" 链 05 §4.0.2)+ 加 "v0.8 关键更正" 引用块 + 架构判断加 "组合根 store 编排应引入 registry";② **§4.1 重写** 从扁平 11 项列表改为 A/B/C 三类归属表(A=SessionDB 直接聚合 5 store eager/lazy 矩阵 / B=旧业务实体 6 store / C=v0.8 工作流域 9+ store 带阶段标注)+ 加 D 已废/特殊类 + ToolConfigStore 同名陷阱说明(文件名 tool-usage-store 含 ToolConfigStore+ToolUsageStore 两类,前者手写 SQL 不基于 SqliteStore)+ **新增 §4.1.1 "store 编排:SessionDB 不是聚合根"** 整节:贴 `index.ts:115-171` 摘录源码 + 解释 v0.8 刻意取舍(3 个 ✅ 优势 + 1 个 ⚠️ 代价 "无 registry / 无自动 DI")+ 交叉链 05 §4.0.3 + ADR-021;③ **§4.4 启动顺序** 从单行线性改为 10 步序列(突出 WikiStore 在 hooks 注册前 new 的关键约束 + CronStore/ProjectStore 工作域 store 互不引用);④ **§8.4 Zustand store 表** 从 10 项扩到 14 项 + data-sync helper 单独行(标"不是 Zustand store")+ v0.8 新增 5 store 加粗 + 各自行数补齐 + 加注解释 "10 个" 是 v0.7 计数。源码逐行核对:`ls src/server/*-store.ts`(22 个文件,减 sqlite-store/key-value/message-store = 19 实际 SqliteStore 子类,但部分文件含多类) / `grep "class " src/server/*-store.ts`(逐个提取 26 个 store 类) / `wc -l` wiki-node-store/session-db/index/db-migration/agent-service/sqlite-store(1698/960/904/1059/1200/393 确认) / `session-db.ts:69-105`(确认 SessionDB 只聚合 KV/Memory/MemoryNode eager + ExtractionCursor/Telemetry lazy = 5 个) / `server/index.ts:113-171`(确认 15+ store 独立 new 序列 + WikiStore 在 hooks 前 new) / `wc -l src/renderer/store/*.ts`(14 store + data-sync = 15 文件) / `ls src/server/agent-tool-store.ts`(确认已删) / `grep "agent-tool-store" src`(仅 router/management-service 注释提及 v0.8 退役,无 import) | docs/arch/02-module-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #9 | 2026-06-26 23:50 | 全文重写 `docs/basic/file-structure.md`(种子 backlog 第二项,定位是"扁平化给新人看的目录速查表",此前所有计数都是 v0.7 旧值,与 src/ 实际严重脱节)。**问题背景**:① "17 个内置工具" —— 实际 ALL_TOOLS 25 entries / 9 categories(#5 已重写 04);② "4 个 feature hook handler:compression/memory/rag/index" —— memory-hooks v0.8 已删,实际 7 个 handler(#1 已修 02/03 的 hook 清单);③ "10 个页面目录" / "10 个 Zustand store" —— 实际 13 个组件目录 / 14 store + 1 helper(v0.8 加了 cron/requirements/skills/wiki 4 域 + project/notification/wiki 4 store);④ "5 个内置 MCP 工具" —— 实际 6 个 mcp-tools 文件(含 cookie-jar);⑤ "16 个 handler 模块"(main 进程)—— v0.8 后 handler 全下沉到 server REST + ipc-proxy 派生,main 只剩 4 个文件;⑥ 完全没提 v0.8 工作流域 9 store(project/requirement/cron/orchestrate/wiki/...);⑦ subagent-delegation.ts 当合法文件列(实际死代码,见 #6)。改:① 顶部定位段补一句"运行时实际 4 个独立 JS 上下文(main/server/renderer/runtime)+ server 是 main fork 的子进程"链到 08 §12;② **核心目录表**从扁平列表改为带链接/职责/关键内容的表格,每层链到 src/ 实际路径;③ **runtime/ 详细结构**拆为顶层文件(逐个补行,标 subagent-delegation 死代码 + subagent-delegator 当前)、hooks(7 个 handler 表 + 注册中心说明 + v0.8 memory-hooks 删除)、tools(25 entries 基础设施 + 按域分组,标 Agent≠AgentRegistry 同名陷阱 + CONDITIONAL_TOOLS 门控)、mcp-tools(6 文件表,标 memory-tools legacy 未注册);④ **server/ 详细结构**重写为按域分组:会话核心(SessionDB 自持 5 表 + 6 聚合 store,链 05 §4.0.3)/ 旧业务实体 / v0.8 工作流域 9 store(标"在 server/index.ts 独立 new,不挂 SessionDB")/ 路由层(20 个 *-router.ts + ROUTE_MAP 派生说明)/ 服务编排(9 个 *-service.ts)/ 基础设施(db-migration 5 阶段链 05 §4.2 + data-change-hub 链 07 §2.3.1 + fresh-db-seed 含 migrateWikiDiskLayout);⑤ **main/ 详细结构**重写为 4 文件(backend-spawn / ipc-proxy 链 07 §2.6 non-2xx reject / test-setup 标 ZERO_CORE_TEST_FIXTURE),显式删"16 handler 模块"叙事;⑥ **renderer/ 详细结构**改 13 组件目录(标 v0.8 新增 4 域)+ 14 store + 1 helper(标 data-sync 是 helper 不是 store);⑦ **命名约定 / 归属规则 / 新增模块该放哪** 三段全部对齐 v0.8:层名表加"进程入口分支文件"、IPC 通道改走 ROUTE_MAP 派生(删手写 ipcMain.handle 旧指引)、持久化实体补"默认在 server/index.ts 独立 new 不挂 SessionDB"、加 feature hook 必须在 hooks/ 注册(强制,不内联 agent-loop.ts)一行;⑧ **维护规则**加一句"计数会随 v0.x 漂移,改之前用 `ls ... | wc -l` 核对,不要照抄本文"。源码逐行核对:`ls src/`(8 顶层)/ `ls src/runtime/`(27 文件)/ `ls src/runtime/tools/*.ts` 减 4 基础设施 = 23 工具文件 + Platform 注入 = 25 entries / `ls src/runtime/hooks/`(7 + index)/ `ls src/runtime/mcp-tools/`(6)/ `ls src/server/`(85 文件)/ `ls src/main/`(4)/ `ls src/renderer/components/`(13)/ `ls src/renderer/store/`(15 = 14 store + data-sync)/ `ls src/shared/`(5)/ `tools/index.ts:70-109`(ALL_TOOLS 25 entries 确认)/ `hooks/index.ts:32-58`(7 个 register*Hooks 确认,memory-hooks 删除确认) | docs/basic/file-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #8 | 2026-06-26 23:25 | 重写 05-persistence.md §4 整章 + §11.2 一行,修两个相互关联的事实错误(backlog #33 + #34,都是源码核对发现的 stale 描述,不是猜测)。**问题背景**:① §4 开篇"当前约 850 行"+"持有 KeyValueStore/MemoryStore/MemoryNodeStore **4 个独立的存储后端**"双重过时 —— v0.8 M5 加了 2 个 lazy store(ExtractionCursorStore/TelemetryStore),且 v0.8 M0~M3 引入的 9 个工作流域 store(ProjectStore/RequirementStore/CronStore/WikiStore/...)在 `server/index.ts:148-171` **独立 new**,根本不挂在 SessionDB 上,所以"4 个后端"既漏了新内核 store 又误把工作流域 store 算进来;② §4.2 "迁移机制 `db-migration.ts:91-223`" 行号过时 —— v0.8 后 `runMigrations` 已扩到 584-1004(420 行),原叙事只覆盖旧 JSON 迁移,**完全漏了 9 张 v0.8 表是怎么被创建的**。改:① **§4 标题**从"业务核心"改为"会话核心(不是业务核心)",明确定位收窄;② **新增 §4.0.1 SessionDB 自持表矩阵**(5 张:sessions/messages/turns/turn_state/tool_executions,各标写入入口);③ **新增 §4.0.2 SessionDB 直接聚合 store 表**(6 个:3 eager 内核 + 2 v0.8 M5 lazy + 说明 lazy 的设计动机"不碰 M5 路径的代码不付初始化成本"+ 两表故意不进 db-migration 的 *_COLUMNS);④ **新增 §4.0.3 关键边界**(v0.8 工作流域 store 在 server/index.ts:159-171 独立 new,把 SessionDB 当 `getDb()` 提供者而非父聚合根;贴 8 行 new 序列源码;解释这个边界是 v0.8 刻意取舍 —— 会话核心不被工作流域写入拖累 + 新增 store 不必改 SessionDB,代价是 server/index.ts 手动编排 12+ store 无 registry);⑤ **新增 §4.0.4 旧叙事更正**段,显式列出此前"4 个后端"说法的两个错误(计数过时 + 误当聚合根);⑥ **§4.2 整段重写**为 5 阶段(阶段 1 列补齐 safeAddColumn 必须先于 SqliteStore 构造 + 列出 agents/providers/sessions/turns 各自补的列 + DROP agent_tools / 阶段 2 v0.8 表 DDL 按 11 步依赖顺序,特别强调 project_wiki 必须先 migrateWikiTableSchema 再 migrateWikiDetailToDisk 否则全局 root 装不进去 + tool_configs 因 PK=tool_name 不进 SqliteStore / 阶段 3 构造 SqliteStore + role_tag 物理列故意补回 / 阶段 4 旧 JSON→SQLite 6 文件含 agent_tools 已删标注 / 阶段 5 KV 6 文件 + Memory)+ 末尾标"v0.8 表无 JSON 前身,阶段 4/5 完全不涉及它们";⑦ **§11.2 session-db.ts 行数** 850→960 + 补一句"拆分压力其实只剩会话核心 5 张表 + 6 个内核 store"(因为工作流域 store 已独立)。源码逐行核对:session-db.ts:37-43,51-108(6 个 store getter 含 2 lazy)/ session-db.ts:960 行 wc 确认 / db-migration.ts:584,1004,289-308,324-416,429-454,474-582(runMigrations 边界 + safeAddColumn/Index + 两个 wiki 迁移子函数) / db-migration.ts:589-847(阶段 1+2 全段) / db-migration.ts:900-1003(阶段 3+4+5 全段) / server/index.ts:148-171(8 个工作流域 store 独立 new,确认不挂 SessionDB) / grep ProjectStore/RequirementStore/CronStore/WikiStore new(全在 server/index.ts,零处在 session-db.ts 内 new) | docs/arch/05-persistence.md | (本次 commit) |
- #7 | 2026-06-26 22:50 | 把 05-persistence.md 从"15 张表 + 重复 ER 图"扩到"30 张表 + 双 ER 图",补齐 v0.8 工作流域 9 张表(此前文档完全没提 projects/requirements/crons/orchestrate_*/project_wiki/wiki_scan_cursors/tool_configs/tool_usage/project_jobs)。**问题背景**:v0.8 M0~M3 落地后 db-migration.ts:653-897 新增了 9 张表,但 05 §2.2 业务实体表还停在 v0.7 的 11 张,§2.13 ER 图既漏新表又有两块完全相同的 mermaid 重复(渲染出两份一样的图)。改:① **§2 标题**:"11 张业务表 + KV + FTS5" → "30 张表:11 业务表 + 9 张 v0.8 工作流域表 + 4 张 SessionDB 内核表 + KV/FTS5/telemetry/cursor";② **§1 目录树** 补 `~/.zero-core/wiki/`(v0.8 P1 §10.1 磁盘镜像树根,project_wiki 正文下沉地),加说明段指 06 §2.5;③ **新增 §2.2b「v0.8 多 Agent 工作流域表」**:14 行矩阵覆盖 projects/project_wiki/wiki_scan_cursors/requirements(×3:主表+history+messages)/task_steps/crons/cron_runs/project_jobs/tool_configs/tool_usage/orchestrate_plans/orchestrate_manifests,每行含 Store 类名+行号+列数+v0.8 阶段(M0/M1/M2/M3/P0)+ 备注;加 ToolConfigStore 手写 SQL 而非 SqliteStore 的说明(PK=tool_name 非 surrogate id);加"与旧业务表边界"段(agents.knowledgeBaseIds vs project_wiki 是两套并行 wiki);④ **§2.13 拆为 2.13a + 2.13b 双 ER 图**:2.13a = 会话/旧业务/记忆(原图,sessions 加 context_project_id 列、turns 加 turn_group 列);2.13b = v0.8 工作流域全新图(projects↔requirements↔task_steps/history/messages、orchestrate_plans↔manifests、crons↔cron_runs、project_wiki self-ref、wiki_scan_cursors、tool_usage/tool_configs/tool_telemetry/extraction_cursors 全部补节点),消除原 duplicate 块;"关键关系"段补 sessions.context_project_id 反向挂 projects、project_wiki project_id 可空、tool_usage≠tool_executions 三条新关系;⑤ **§3** "9 个 Store 的地基" → "18 个 Store 的地基(v0.8 9 张表全复用)";⑥ **§10 性能表** 补 v0.8 索引(idx_turns_session_group / idx_wiki_parent_path / idx_tool_usage_*)+ 备注"v0.8 只加了 step-group 索引,session+seq 全读仍未索引";⑦ **§11.1** 加两条做对了(v0.8 表全 DB-native 省迁移 / safeAddIndex 幂等封装);⑧ **§11.2** 加三条可改进(三套 wiki 并存 / 30 张表导出更紧迫 / tool_usage↔tool_executions 语义重叠应合并 / db-migration.ts 1059 行 DDL 应下沉各 store)。源码逐行核对:db-migration.ts:653-897 全段 9 张 CREATE TABLE + safeAddIndex / project-store.ts:78 / requirement-store.ts:85,93,94 / cron-store.ts:63,176 / orchestrate-store.ts:79,131 / project-job-store.ts:47 / tool-usage-store.ts:51,129(确认 ToolConfigStore 手写 SQL 非 SqliteStore)/ task-step-store.ts:58 / wiki-node-store.ts:327 / wiki-scan-cursor-store.ts:62 / telemetry-store.ts:97 / extraction-cursor-store.ts:77 | docs/arch/05-persistence.md | (本次 commit) |
- #6 | 2026-06-26 22:20 | 修 4 篇 arch md 里关于"子 Agent 委派文件"与"test-seed 边界"的事实错误(都是源码核对发现的 stale 描述,不是猜测)。**问题背景**:`subagent-delegation.ts`(329 行)与 `subagent-delegator.ts`(413 行)是 v0.8 委派重构前后的同义文件,重构后前者零 importer(死代码),后者是 AgentLoop 实际实例化的 `SubagentDelegator` 类;但多份文档把两者关系写反或写糊。改:① **02 §3 表**:把两行从"subagent-delegation=当前/delegator=旧"纠正为"delegator=当前(413 行,被 agent-loop.ts:52 import + Agent 工具调用)/delegation=死代码(零 importer,删除候选)";② **02 §2.1 边界约束**:原"`test-seed.ts` 有 type-only import,属于测试辅助例外"不准确 —— test-seed 既有 5 处 import type(server stores),**也有 1 处值导入** `ensureWikiSkeleton`(from `../server/fresh-db-seed.js`);重写为分层说明(类型层 = 编译时擦除 / 值层 = 真运行时边但生产路径门控不可达)+ 解释为什么不能挪到 tests/(需在 backend 子进程内加载 better-sqlite3 ABI);③ **02 §2 表** `test-seed.ts` 从 `n/a | 测试种子` 改为 `187 | 测试种子(ZERO_CORE_TEST_FIXTURE gated,见 §2.1)`(补真实行数 + 标注门控);④ **03 §1 表**:原"`subagent-delegation.ts`(326):已废,委派统一走 Agent action 工具"两处不准 —— 行数实际 329、且"统一走 Agent 工具"是把"对外工具接口"与"运行时实现"两层混在一起;改为两行:`subagent-delegator.ts`(413,当前)+ `subagent-delegation.ts`(329,死代码);⑤ **03 §3.1 委派模型**:加一段澄清"Agent 工具(协议层)→ SubagentDelegator(运行时层)"两层分工(Agent 工具内部调 `SubagentDelegator.delegateTask()`,后者才是真正建子 loop 跑任务的),并加"子 Agent 上下文隔离 v0.8 关键修复"小节(sessionId=undefined → 不 rebuildFromTurns 父历史 + 不写父 turn 表,消除跨 agent 写竞争);⑥ **08 §2.4 事件分类表**:"子 agent | SubagentStart/Stop | subagent-delegation" 改为 "subagent-delegator(delegateTask/delegateTaskBackground/stopTask 内 triggerHooks)";⑦ **12 术语表**:把单条 "subagent-delegation:子 Agent 委派工厂" 拆为两条 —— `subagent-delegator`(当前类,加完整定义 + hook 列表)+ `subagent-delegation`(历史,死代码,删除候选,显式提示"不要与 delegator 混淆")。源码逐行核对:`grep "from .*subagent-delegation" src`(零命中)/ `grep "from .*subagent-delegator" src`(命中 `agent-loop.ts:52`)/ `grep "test-seed" src`(命中 `main/test-setup.ts:29-30` 值 re-export)/ `test-seed.ts:31-41` 导入分析 / `subagent-delegator.ts:82-236,344-408`(确认 SubagentStart/Stop 在 delegateTask/delegateTaskBackground/stopTask/runBackground 内 triggerHooks)| docs/arch/02-module-structure.md, docs/arch/03-runtime-engine.md, docs/arch/08-cross-cutting.md, docs/arch/12-glossary.md | b31c4c4 |
- #5 | 2026-06-26 21:51 | 重写 04-tools-subsystem.md 与 v0.8 工具子系统对齐(此前文档仍是 ≤ v0.7 视角,21 个工具 + buildAgentTools internal/CLI 二分)。改:① §1 标题与正文从"三层工具分类(built-in/MCP/Agent-as-Tool)"改为"物理来源(2 层) + 语义 category(9 类)",mermaid 图重画,补 v0.8 重要变更说明(原 Agent Tools 第三层取消 + 8 个 zero-admin → 4 action 工具 + Assistant→Platform 改名);② §2 buildTool 行号 92-211 → 163-273;新增 §2.0「buildTool 内置的横切关注点」(PreToolUse 阻断 hook / rateLimiter / 真正 execute / PostToolUse+PostToolUseFailure hook / recordToolUsage 遥测打点 v0.8 P3 §7.7 #4 / truncateResult),这是 v0.8 关键架构提升,此前文档完全没提;③ §2.2 extractInputFields 行号 249-273 → 324-349,补 enum 字段;④ §3 矩阵从 17 行扩到 25 行:新增 CreateRequirement/CreateRequirementWithDoc/Orchestrate/verify(workflow 域)+ Project/AgentRegistry/Cron/Wiki/Platform(management 域),Assistant 改 Platform 并加 RENAMED_TOOLS 说明,Wait 从 task 改为正确的 runtime(核对 src),每行加 CONDITIONAL 门控列,补 v0.8 memory-hooks 删除与同名陷阱(Agent ≠ AgentRegistry)说明;⑤ §5 完全重写——原 buildAgentTools internal/CLI 二分已废,改为 §5.1 Agent 委派工具(agent.ts,action=list/delegate,v0.8 sessionId=undefined 隔离修复跨 agent 写竞争)+ §5.2 AgentRegistry 注册表 CRUD(agent-tool.ts,7 个 action,toolPolicy MERGE 语义,zero 保护,summary 防 context 泛滥);⑥ §6 buildToolsSet 签名从 4 参数改 3 参数(agentTools 参数已删),补"tools map 优先于 autoApprove"的隐含契约;⑦ §8 执行链 mermaid 时序图重画——加 PreToolUse blocked 分支、rateLimiter acquire/release、recordToolUsage 打点(成功/失败两条路径)、truncateResult,删除原 ctx.emit(tool_end) 误述;⑧ §9 安全表删 stale 路径 assistant-tools.ts:29 BLOCKED_FILES(文件已不存在),改写 Platform redactSensitive 输出层、加 PreToolUse 阻断与子 Agent 委派隔离两行、修 evaluateToolCall 行号 core/tool-policy.ts:37;⑨ §12.1 加 buildTool 包装层 / action 合并 / CONDITIONAL 自动收口 / 委派隔离修复 4 项做对了;§12.2 删"internal 共享 DB 写竞争"陈旧评价(v0.8 已修),改为 requiresConfirmation 死字段 / 同名陷阱 / redactSensitive 是输出层补丁 / evaluateToolCall 与 buildToolsSet 双处读 blockedTools 的 drift 风险;⑩ §13 一图总览 21 entries → 25 entries/9 categories,加 buildTool 包装层节点 + RENAMED_TOOLS 节点,标注 agentTools 参数已取消。源码逐行核对:tools/index.ts:70-110,112-126,140-160,157,163-241,195-203 / tools/tool-factory.ts:58-67,163-273,178-235,274,324-349 / tools/agent.ts:1-60,46 / tools/agent-tool.ts:138-260,165,183 / core/tool-registry.ts:65-76,76(RENAMED_TOOLS) / core/tool-policy.ts:37-70 / mcp-tools/platform-tools.ts(全文,redactSensitive) / mcp-tools/fetch-tools.ts:487(WebFetch category) / mcp-tools/sequential-thinking-tools.ts:26-75 | docs/arch/04-tools-subsystem.md | 7f8eac1
- #4 | 2026-06-26 21:20 | 补 08-cross-cutting.md 缺失的 v0.8 后端子进程生命周期 + 全局错误兜底。新增 §12(7 小节 + mermaid flowchart):§12.1 为什么后端是子进程(better-sqlite3 ABI 隔离,dev spawn / packaged fork)、§12.2 启动握手协议(--port=0 + stdout JSON ready 行 + 30s 超时 + stderr 桥接到 logger)、§12.3 自愈(_shuttingDown flag 防竞争 + 重启 fire-and-forget + 端口会变)、§12.4 优雅关闭三段式(stdin shutdown → 5s SIGTERM → 3s SIGKILL)+ Windows SIGTERM/SIGKILL 无语义标注、§12.5 process.on 兜底(只 log 不 exit 的取舍 + stderr→logger 链路)、§12.6 端口暴露与 ipc-proxy 关系、§12.7 架构师评价(4 做对了 + 4 可改进)。同时修正 §2.5 已注册 handler 表(删已废 memory-hooks,补 notification/provider-options/todo-cleanup/extraction/tool-execution hooks,补注册顺序说明)、§2.2/§2.3/§2.6 把 first-writer-wins 改为正确的 "blocked 短路 + 字段 merge last-writer-wins"(与 02/03 #1 一致)、§2.3 时序图重画、§11 安全表 Renderer↔Backend 行从"约 140 代理通道"改为 v0.8 实际(120+ 代理 + 17 LOCAL + 3 INVOKE_NOT_PROXIED + ROUTE_MAP 派生)、§13 清单加两行(后端生命周期 ✅ / 全局错误兜底 ⚠️)。源码逐行核对:backend-spawn.ts:36,56-132,134-164,166-168 / server/index.ts:86-96,110-128 / runtime/hooks/index.ts:32-60 | docs/arch/08-cross-cutting.md | 782c1c3
- #3 | 2026-06-26 20:51 | 对齐 07-renderer-and-ipc.md 与 v0.8 IPC 层实际。改:① §1 store 计数 10→16(含 data-sync helper + project/requirement/wiki/cron/notification stores);② §2.2 R 表从"约 140 项"改为"约 120+ 项,按 26 个域分块注释"+ buildReq 的 params/body/query 三出口说明 + path 占位 encodeURIComponent;③ §2.3.1 data-change-hub 流程图细化(白名单 gate / coalesce / 单独 win.send 防 agent:event 污染)+ 新增"实际订阅矩阵"表(5 collection × 5 store,标 subscribeDataChange vs subscribeListDataChange + 增量策略);④ §2.5 重写"契约例外"——三组集合(LOCAL_CHANNELS 17 项含 orchestrate/pm-handlers / INVOKE_BUT_NOT_PROXIED 3 项 / 退役 agent-as-tool 反向断言)+ 关键澄清 ROUTE_MAP 是测试从 ipc-proxy.ts 源码正则派生而非手写常量 + 标注 search-provider:* 已 v0.8 清理;⑤ 新增 §2.6 v0.8 non-2xx reject(ipc-proxy.ts:324-337,过去静默 resolve 吞 4xx/5xx,现在 throw 带 status+path+excerpt);⑥ §4 AppLayout handler 表 11→14(补 ask_user / requirement_notification / step_failure / verification_failure + message_end 不带 usage 的历史 bug 说明);⑦ §3.2 store 列表补全 v0.8 工作流域 store;⑧ §7 方法计数 150→149 + ROUTE_MAP 派生说明;⑨ §12.2 改写"手写契约"评价 + non-2xx 落地但调用方 try/catch 仍欠缺。源码核对:ipc-proxy.ts:51-274,278-349,355-406 / data-change-hub.ts:31-107 / data-sync.ts:30-70 / rest-routers.test.ts:480-527 / preload/index.ts:87-110,171-174 / AppLayout.tsx:100-205 / store/*.ts 订阅点(agent:136/cron:127/project:90/requirement:184/wiki:198) | docs/arch/07-renderer-and-ipc.md | 15c0d6f
- #2 | 2026-06-26 20:35 | 在 06-knowledge-subsystems.md 新增 §2.5「Wiki 体的磁盘镜像树布局(v0.8 P1 §10.1)」+ §2.6「archivist 增量扫描与摘要懒加载(v0.8 M2)」。§2.5 覆盖:WIKI_DISK_ROOT 隔离根 + isInsideWikiDisk FS 隔离、diskPathFor 路径推导规则表(folder=目录/leaf=文件,global/container/subtree/regular 四类)、leaf→folder promote、改名/reparent 时正文 rename 跟随、migrateWikiDiskLayout 启动一次性幂等迁移、writeNodeDetail/readNodeDetail 永远走推导路径不信任 docPointer、拆库+镜像树的动机。§2.6 覆盖:WikiSkeletonService 命名澄清、buildSkeleton/rescanProjectFull 入口、(archivist,project) git cursor 增量(mermaid 时序图)、ensureSummary 懒加载物化(扫描期零读盘 / 首次 expand 付钱)、唯一调用方 + scope/type 护栏。源码逐行核对:wiki-node-store.ts:197,274,447-469,471-510,584-627,637-740,757-819 / fresh-db-seed.ts:325-337 / wiki-skeleton-service.ts:1-60,160-250,460-540,630-660 / archivist-git.ts:10-15 | docs/arch/06-knowledge-subsystems.md | 5f4dd02
- #1 | 2026-06-26 19:45 | 新增 Hook 系统专节(执行模型 last-writer-wins + blocked 短路 + 错误吞掉、事件→触发点→handler 全景表、PreLLMCall 现查+注入模式、含 mermaid 时序图);修正 02 §3.2 hooks 文件清单(删 memory-hooks、补 notification/provider-options/todo-cleanup/extraction、改注释为实际注册顺序)与 §2 表 hook-registry 描述(改 "first-writer-wins" 为正确的 "last-writer-wins merge + blocked 短路");02 §3.2 加 v0.8 memory-hooks 删除说明。源码核对:hook-registry.ts:78-91 / hook-types.ts:29-39 / hooks/index.ts:47-59 / agent-loop.ts:228,251,296,312,466,497,678,705,726,766 | docs/arch/02-module-structure.md, docs/arch/03-runtime-engine.md | 08117f6

## Open questions(给用户的)
<!-- 拿不准的写这里 -->
- **[new, #7]** `tool_usage`(v0.8 P0 §7.7 #4)与 `tool_executions`(旧 §2.1)语义高度重叠 —— 都是工具调用日志,只是字段集不同(`tool_usage` 有 params+独立 session_id 适合 cron/后台,`tool_executions` 有 input/output_preview 适合 UI 展示)。是否要合并成一张表 + 视图,还是保持两套各司其职?需用户拍板(合并要改 5+ 消费方)。
- **[new, #5]** `meta.requiresConfirmation` 字段在 buildTool/tool-registry/registerRuntimeTools 三处都已贯穿(写进 ToolMeta),但 `agent-loop.ts` 不读它——是死字段。是否要落地一个 PreToolUse handler 把它真正接起来(执行前 AskUser 弹窗)?这会改变"工具自动执行"的默认体感,需要用户拍板默认行为(哪些工具默认 ask、哪些不 ask)。
- **[new, #5]** `Agent`(委派) vs `AgentRegistry`(注册表 CRUD)两个工具名前缀相同,LLM 容易混淆(工具描述里已加一句澄清)。长期是否把 `AgentRegistry` 改名为 `RoleRegistry` / `ManageAgents` 更直观?需权衡 RENAMED_TOOLS 迁移成本 + 用户已有 toolPolicy 配置。
- **[new, #5]** `evaluateToolCall`(core/tool-policy.ts) 与 `buildToolsSet`(tools/index.ts) 双处独立读 `blockedTools`,数据源一致但维护时易 drift。是否要合并成单一真值源?
- **[new, #4]** backend 自愈(`backend-spawn.ts:117-130`)目前无重启计数/退避:连续崩溃会无限静默重启,用户感知不到 backend 死了。是否要加"N 次失败后通知主进程弹用户提示"?
- **[new, #4]** `server/index.ts:86-96` 的 `process.on("uncaughtException", ...)` 只 log 不 exit。这意味着 backend 即使状态已损坏仍继续服务(可能写脏数据)。是否应改为"log + 主动 `process.exit(1)`"让 §12.3 自愈路径接管?权衡是频繁重启可能比脏跑更糟,需要用户拍板默认行为。
- 暂无历史遗留。#1~#5 改进全部基于源码逐行核对,无猜测。
