# 一夜文档循环 · 追踪文件

> 跨触发记忆。每次全新 session 读这里接着干。只有这个文件 + charter 是状态来源。

## Vision
把 zero-core 的 docs/ 做得更清晰、更详实、结构更好、更多可视化/可交互。允许打破现有结构。
独立分支 docs/overnight-20260625,明早人审后合并。

## 规则摘要
- 只动 docs/。每次一个连贯改进就 commit 收工。
- 不 push/merge/动源码/跑构建。
- commit 带 Co-Authored-By: Claude <noreply@anthropic.com>。

## 候选 backlog(初始种子,按价值排序,可改)
- [x] **[new, #24]** 扫描 `docs/arch/09-extension-points-and-adrs.md` / `10-tech-debt-architect-view.md` 是否有同类 v0.7 残留(MemoryStore 活跃叙事 / requiresConfirmation 修复项 / 旧 IPC 通道计数 / `recovery.ts` 文件引用等)。#24 修了 11-quality-attributes.md,但 09 / 10 这两篇一直没人扫过,可能也有同样的 v0.7 事实错误。优先级中(09 是 ADR 集合,10 是技术债视图,二者比 11 更容易被维护者长期参考)。
- [ ] **[new, #24]** 11-quality-attributes.md §9 quadrantChart 的坐标是 v0.7 时期的主观评估。v0.8 引入 14 张工作流域表 + 25 工具 + ROUTE_MAP 派生 + buildTool 横切关注点后,"DX(开发体验)" 应该明显右移(加工具 / 加表成本下降),"可演进性" 也应上移。下次扫 11 时可重评坐标 + 在结论段补一句"v0.8 后 DX 评估应上调"。优先级低(主观图,不影响事实)。
- [ ] 核对 docs/arch/02-module-structure.md 与 src/ 实际模块树,补/改过时路径
- [x] 核对 docs/basic/file-structure.md 与当前目录布局 —— 见 #9(全文重写,所有计数按实际 src/ 树核对:17→25 工具 / 10→13 组件目录 / 10→15 store / 4→7 hook / 5→6 mcp-tools / 删"16 handler 模块"叙事 / 补 v0.8 工作流域 9 store 分组 + main 4 文件 + subagent-delegation 死代码标注)
- [x] docs/arch/03-runtime-engine.md:补厚 AgentLoop / hook 机制(PreLLMCall/PostTurnComplete),加 mermaid 序列图 —— 见 #1
- [x] docs/arch/04-tools-subsystem.md:工具注册/路由/权限模型 + 25 个工具 v0.8 矩阵 + buildTool 横切关注点(hook+遥测+限流)+ Agent vs AgentRegistry 区分 —— 见 #5
- [x] docs/arch/05-persistence.md:SqliteStore 通用 CRUD + 各 Store + migration,加 ER 图(mermaid) —— 见 #7(原表只覆盖 11 张业务表,ER 图漏了 v0.8 9 张工作流域表 + duplicate 块;本次补 §2.2b 新表矩阵 + 拆 §2.13 为 2.13a/2.13b 双 ER 图;§3/§10/§11 同步)
- [x] docs/arch/06-knowledge-subsystems.md:wiki 目录镜像树 + archivist 增量扫描 + 摘要懒加载(v0.8 本次刚改),务必同步 —— 见 #2
- [x] docs/arch/07-renderer-and-ipc.md:data-change-hub + data-sync + IPC ROUTE_MAP 派生 + non-2xx reject —— 见 #3
- [ ] docs/visualization/code-graph-data.json 与 src/ 当前结构对齐(27k 行,需谨慎,优先核对顶层节点而非全量重生成)
- [x] docs/arch/08-cross-cutting.md:后端子进程生命周期 / 全局错误兜底(v0.8 本次刚加) —— 见 #4
- [x] 新增 docs/visualization/ 下可交互页:数据流(data-change-hub 推送) —— 见 #23(自包含交互式 `data-sync-flow.html`:5 段泳道主图 + 6 情景演练 + 节点点选详情卡;覆盖 emit/coalesce/flush/broadcast/store 自订阅全链路;顺手在 07 §2.3.1 加交叉链接)。**wiki 懒加载树演示仍未做,留作下次**(独立项,逻辑与数据流完全不同)
- [x] docs/arch/02-module-structure.md §2.1 边界约束提到 "test-seed.ts 有 type-only import" —— 核对是否仍准确 —— 见 #6(实际还有 `fresh-db-seed` 值导入,但生产路径门控不可达)
- [x] **[new, #8]** 05 §4.0.3 新叙事("v0.8 工作流域 store 在 server/index.ts 独立 new,SessionDB 只当 getDb() 提供者")应反向同步到 02-module-structure.md —— 见 #10(02 §4 / §4.1 / §4.1.1 / §4.4 / §8.4 全改:文件规模表对齐 v0.8 行数 + 重写"11 个 SqliteStore"为 A/B/C 三类 + 新增 §4.1.1 编排边界 + 启动顺序 10 步化 + renderer store 10→14+1)
- [ ] **[new, #10]** 03-runtime-engine.md 若提到 SessionDB / store 依赖注入 —— 核查是否也停留在 v0.7 "SessionDB 是聚合根"叙事(grep 已确认 03 当前**零** "SessionDB"/"聚合根" 命中,可能是真没问题,但下次扫 03 时顺手核对 store 相关段落是否需要补 v0.8 编排边界链接)
- [x] docs/arch/03-runtime-engine.md §1 表 "subagent-delegation.ts(326) 已废" —— 核对 v0.8 Agent action 工具落地后该文件是否还在 src/ 实际被 import —— 见 #6(确认零 importer,死代码;但委派实走 `subagent-delegator.ts` 的 SubagentDelegator 类,不是"统一走 Agent 工具"那么简单)
- [x] docs/arch/07-renderer-and-ipc.md §3.7 mermaid 拓扑图仍是 v0.7 store 集合 —— 下次可补 project/requirement/wiki/cron/notification store 节点 + data:changed 边(本次只改文字,未动图) —— 见 #11(已重画为双路径图:StreamEvent→AppLayout + data:changed→各 store 自订阅;补 5 个 v0.8 store 节点 + 4 个工作流域页面 + 删过时 AgentToolStore + 修 ChatStore 字段计数 11→8)
- [ ] docs/arch/04-tools-subsystem.md 或 08-cross-cutting.md:核对 orchestrate-handlers.ts / pm-handlers.ts 的 main 进程单例(ConfirmRegistry / PmService)为什么必须留主进程而不能下放 REST(07 §2.5 ① 提到但未展开)—— **08 §12.1 已部分回答(ABI 边界),但 ConfirmRegistry/PmService 单例本身未展开,留作下次**
- [ ] docs/arch/08-cross-cutting.md §12.3 自愈无退避策略 + §12.5 uncaughtException 不 exit —— 已在 §12.7 评价里标 open question,下次可补"重启计数 + 通知主进程弹窗"的具体设计提案(若用户认可)
- [x] docs/arch/08-cross-cutting.md §2.4 "30 个事件 / 23 个未装载" 计数来自旧版,#1 已在 02/03 修正 hook 清单 —— 08 §2.4 的具体事件表还是旧的(本次只改了 §2.2/§2.3/§2.5/§2.6,没动 §2.4 表),下次可把 §2.4 表与 02 §3.2 实际注册清单对齐 —— 见 #14(§2.4 表逐行核对:30 事件按"实际触发 / 仅定义"二分,20 个真活跃 + 10 个零触发幽灵;§2.6 评价同步从"23 个未实现"改为"10 个零触发";每个事件标 file:line 触发位置)
- [x] **[new, #6]** `code-graph.html`(内联 27k 行 DATA)与 `code-graph-data.json` 都把 `subagent-delegation.ts` 与 `subagent-delegator.ts` 作为合法节点列出 —— 与 #6 在 arch md 里建立的"前者是死代码"叙事不一致。理想做法:在 `code-graph-data.json` 给 `subagent-delegation.ts` 加 `"dead": true` / `"supersededBy": "subagent-delegator.ts"` 元数据,code-graph 渲染时灰显并标删除候选。 —— 见 #15(数据层:两个数据源 JSON + HTML 内联 DATA 都加了 `dead:true`/`supersededBy` + 改描述;渲染层:CSS 加 `.dead-badge`/`.edge-list li.dead`/`.superseded` 样式 + showFileDetail 头部 DEAD CODE 徽章 + "已被取代 →" 提示 + showDirDetail 文件列表灰显+内联 DEAD 徽章 + renderFileBrowse Calls 面板灰显;同时修了同文件 #34 test-setup.ts imports 缺边)
- [ ] **[new, #15]** `code-graph-data.json` 现在只有 1 个文件节点带 `dead:true`(subagent-delegation.ts)。src/ 里可能还有别的零 importer 候选(如 #5 提到的"已删 memory-hooks"、arch 里标注的其他死代码)—— 下次触发可写一个 `node` 一次性扫描脚本(用 `grep -L "from .*X"` 验证零 importer),给所有真死节点批量加 `dead:true`,让 code-graph 成为可信的死代码雷达。需用户认可"死代码灰显"作为 code-graph 的长期产品定位。
- [x] **[new, #6]** `code-graph.html` 内联 DATA 里 `test-setup.ts` 标 lines=31 且 imports=[],但实际它 re-export 了 `test-seed.ts` 的值(`isTestMode`/`seedTestEnvironment`/`TestSeedResult`)—— 内联数据 imports 缺这条边。 —— 见 #15(JSON + HTML 内联 DATA 两个数据源都补了 `exports:[isTestMode,seedTestEnvironment,TestSeedResult]` + `imports:[src/core/test-seed.ts]` + 改描述澄清 facade 角色;src/core/test-seed.ts 已是合法节点,边有目标)
- [x] **[new, #7]** 05 §4.2 "迁移机制" 行号还写 `db-migration.ts:91-223`,但 v0.8 后该函数已扩到 1059 行(91-897+) —— 见 #8(已重写为 5 阶段:列补齐 / v0.8 表 DDL / SqliteStore 构造 / JSON→SQLite / KV+Memory,正确行号 `runMigrations` 在 584-1004)
- [x] **[new, #7]** 05 §4 提到 SessionDB 持有 KeyValueStore/MemoryStore/MemoryNodeStore,但 v0.8 后 SessionDB 还应该持有(或被注入)ProjectStore/RequirementStore/CronStore/WikiStore 等 —— 核对 session-db.ts 实际是否聚合这些 store,还是各自独立 new —— 见 #8(**核对结果**:SessionDB **不**聚合工作流域 store —— 只有 3 个 eager 内核 store + 2 个 v0.8 M5 lazy store(ExtractionCursorStore/TelemetryStore);9 个工作流域 store 在 `server/index.ts:148-171` 独立 new,把 SessionDB 当 `getDb()` 提供者用)
- [x] **[new, #7]** 06 §2 已记 project_wiki 磁盘镜像树,但 05 §2.2 旧表里 kb_entries/kb_chunks 与 project_wiki 的"三套 wiki 并存"关系(§11.2 新提)在 06 没有专门小节对比 —— 可在 06 加一节"三套知识系统对比矩阵"(kb_*/memory_nodes/project_wiki 各自的写入触发器 / 检索方式 / 适用场景)。 —— 见 #16(06 新增 §2.7:物理位置 mermaid 图 + 11 行横向对比矩阵 + 三代演进地层叙事 + 维护者提示)
- [x] **[new, #16]** 05 §2.2 标题写"30 张表",但这只数了 db-migration.ts 管理的表。实际 sessions.db 还有 **4 张构造时自建的表不在 db-migration**:`memory_nodes` / `memory_subjects` / `memory_edges` / `memory_nodes_fts`(由 `MemoryNodeStore.init()` 自建,`memory-node-store.ts:138-180`)。这 4 张是 sessions.db 实际存在但文档叙事遗漏的表。下次做 05 时可在 §2.2 标题或开篇加一句"另见 06 §2.7 矩阵:multiply-self-init 表(memory_* 4 张)不进 db-migration,故本表不计"。(纯交叉引用澄清,小改) —— 见 #18(扩成 5 处连环错一起修:db.sqlite→sessions.db 文件名 / kb_chunks 跨库 / memory_* 构造自建 / §2.5 FTS DDL 错列+错 trigger 叙事)
- [x] **[new, #18]** #18 修了 05-persistence.md 里所有 `db.sqlite`→`sessions.db` 文件名错误,但 `grep "db.sqlite" docs/` 可能还有其他文档(02-module-structure.md / file-structure.md / 其他 arch md)残留同样的旧文件名。下次触发可扫一遍 docs/ 全量,把所有 `db.sqlite` 改成 `sessions.db`(或显式标"旧名"如果是在讲历史迁移)。优先级低(只是文件名,不影响理解),但跨文档一致性有价值。 —— 见 #19(`grep "db\.sqlite" docs/` 全量扫:实际残留仅 `docs/arch/01-system-overview.md` 3 处(ASCII 图 / 文件表 / 单图总结) + `05-persistence.md:197`(已在 v0.8 更正 callout 里刻意保留作为"旧名"叙事)+ TRACKER 历史日志。02 / file-structure / 其他 arch md 都已正确。本次 #19 一并修了 01 的 3 处残留)
- [x] **[new, #18]** #18 修 §2.5 FTS5 时发现旧版 DDL 块写了不存在的 `type` 列 + 编了不存在的"trigger"同步机制 —— 这种"看起来可信但与源码不符的 DDL/机制描述"可能在其他章节也有(尤其是 §2.1~§2.5 这种带 SQL 代码块的)。下次扫 05 或其他带 DDL 的文档时,顺手把每个 SQL 块跟 src/ 里的 `CREATE TABLE` 源逐字核对一次(列名/列顺序/约束/索引),不要相信文档里的 SQL 是真的。优先级中(错误的 DDL 会直接误导改 schema 的人)。 —— 见 #20(扩成 §2 / §2.2 / §2.2b / §3 / §4.0.3 / §11.1 / §11.2 一串表计数 + 退役标注连环修:把"30 张表 + memory_* 4 张自建"重算成"33 张 = 批 A 24 + 批 B 9"+ 标退役 `agent_tools` + 漏数 v0.8 工作流域表 9→14 + 漏数 memory_* 自建表 4→6 + 补 ToolUsageStore 手写说明 + §4.0.3 "9 张"叙事也对不上)
- [ ] **[new, #8]** 05 §4.0.3 新叙事("v0.8 工作流域 store 在 server/index.ts 独立 new,SessionDB 只当 getDb() 提供者")应反向同步到 02-module-structure.md(它对 SessionDB / store 的分层描述可能还停留在 v0.7"SessionDB 是聚合根"叙事)和 03-runtime-engine.md(若那里提到 store 依赖注入)。下次做 02 核对时一并查这两篇。
- [ ] **[new, #8]** 05 §4.0.2 表里 ExtractionCursorStore/TelemetryStore 标 lazy,但 07-renderer-and-ipc.md §3.2 的 store 列表若也提这两个,应同步标"lazy + 不进 db-migration"的注释。优先级低,纯交叉引用对齐。
- [x] **[new, #9]** `docs/basic/` 其他几篇(app-flow.md / backend-structure.md / tech-stack.md)可能也有同类 stale 计数(#9 只修了 file-structure.md)。下次可统一扫一遍 basic/ 把过时数字 / 路径修对(优先级低,本系列定位是速查表)。 —— 见 #17(三篇速查表全文过时点修复:tech-stack 删 OpenPrd 章节 + 重写 scripts 表对齐 package.json 实际 11 个脚本 / backend-structure 修 7 处 v0.7 残留 + 路由表补 6 个 v0.8 路由 + 删 AgentToolStore + typed-ipc→ipc-proxy ROUTE_MAP 派生 / app-flow 修 memory-hooks 删除 + agentToolStore.cleanupOrphans→project_wiki 孤儿子树)
- [ ] **[new, #17]** backend-structure.md §"数据流 → IPC 模式" 第 73-79 行那个 ASCII 数据流图(`用户操作 → renderer → store action → preload IPC → main typed-ipc/*-handlers → server store` )与 #17 修的"typed-ipc 已不存在"叙事仍有残留 —— 图里写的是 v0.7 的 `typed-ipc / *-handlers.ts`,实际 v0.8 走 `ipc-proxy.ts` 的 `registerProxyHandlers` + fetch→backend HTTP。下次扫 backend-structure 时可把这个数据流图也改成 v0.8 两跳(fetch→localhost:port→REST router→store)。优先级低(文字段已修,只是 ASCII 图没动)。
- [ ] **[new, #9]** file-structure.md §"server/ 详细结构" 的 store 分组用了域(domain)切分,但 [`02-module-structure.md`](../arch/02-module-structure.md) 的 server 章节可能还是按文件名字母序或老 v0.7 分组 —— 若用户认可 domain 切分更清晰,可把 02 的 server 部分也改成同构分组(避免维护者要在两套分组间脑补映射)。下次做 02 核对时一并考虑。**注:#10 已部分处理** —— 02 §4.1 改成 A/B/C 三类(按归属方式 = 是否挂 SessionDB + v0.8 阶段),与 file-structure.md 的"域"切分(会话核心/旧业务/工作流域/路由层/服务编排)不完全同构,但是两种合理的正交切分(归属 vs 域),下次若统一可考虑用归属维度为一级、域为二级。
- [x] **[new, #10]** 05 §4.0.2 标题写"6 个,分两批",但表格只列 5 个 store(3 eager + 2 lazy),正文 §4.0.2 第 529 行又把 WikiStore 算作"+1 个全局 wiki store 入口"凑成 6 —— 实际 WikiStore 在 `server/index.ts:122` 独立 new,**不在 SessionDB 内**。标题应改为"5 个(3 eager + 2 lazy)",正文那句"+1 个全局 wiki store 入口"是错的(混淆了 index.ts:122 的 WikiStore 与 SessionDB 内聚合)。下次触发可修(改 05 一个文件即可,小改)。 —— 见 #12
- [x] **[new, #10]** 02 §8 components/ 目录树(11 个目录)与 #9 的 file-structure.md(13 个目录)计数不一致 —— file-structure.md 是 13,02 是 11,差 2 个可能是 v0.8 新增的 project/requirement/wiki/cron 组件目录或 dashboard/workspace 的差异。下次触发核对 `ls src/renderer/components/` 实际并对齐 02 §8 列表。 —— 见 #13(02 §8.2/§8.3 全改:11→13 目录、删幽灵 `workspace/`、补 cron/requirements/wiki 3 个 v0.8 目录、agents section 计数 5→6 + 重列、settings section 计数 9→7、common 补 NotificationToast、dashboard 删"待规划")
- [ ] **[new, #19]** ipc-proxy 代理通道计数文档不一致:`01-system-overview.md` §3.3 写"约 140 项"(实际 `R: Record<string, RouteMapping>` 经 brace-match + 顶格 quoted key 计数 = **141**,01 的数字对);但 `07-renderer-and-ipc.md` §2.2 写"约 120+ 项,按 26 个域分块注释"(#3 改的)。两个数字指的不是同一概念 —— 01 的 141 是 `R` 表的代理映射项数(IPC 通道→REST 路径),07 的 120+ 可能是唯一 REST 路径数(127 app.use 路由,#17 计数)或去重后的域数。下次扫 07 时可在 §2.2 显式标"R 表 141 项代理 / 127 REST 路由 / 26 个域分块"三个数,避免读者把 141 和 120+ 当冲突。优先级低(纯计数澄清)。
- [x] **[new, #20]** **跨文档扫描"两套并行 memory 表"叙事是否还有 v0.7 残留**:#20 核对源码时确认 `MemoryStore`(`memory_entities`/`memory_relations`)+ `MemoryNodeStore`(`memory_nodes`/`memory_subjects`/`memory_edges`/`memory_nodes_fts`)是**两个并行系统**(前者 MCP memory-tools 后端、后者会话压缩回退路径),数据不互通。但 #19 的日志里曾把 `memory_entities`/`memory_relations` 笔误为"v0.7 名,实际是 memory_subjects/memory_edges"(实际两套都活着,只是 5 张表里 2 张归 MemoryStore / 3+1 张归 MemoryNodeStore)。需扫其他 arch md(02 / 06 / 08 / 12-glossary)是否也有"memory_entities 已废"或"MemoryStore 已删"的同类错叙事,统一为"两套并存"的 v0.8 视角。优先级中(读者会被误导去删一个活着的 store)。 —— 见 #21(**扫描结果**:02/06/08 都没"已废"错叙事,#20 担心的传染实际只在 12-glossary —— 12 有一处 mermaid 图把 K4(MemoryNodeStore)→K5(MemoryStore) 画成依赖边(实际是兄弟,无任何引用),且两个术语条目都停在 v0.7"旧版/兼容层"笼统措辞,既没说清"僵尸 vs 活"也没说清二者无依赖。本次修了 12-glossary 一篇。**源码核对推翻了 #20 的"两套都活"措辞**:MemoryStore **不是活**,是僵尸 —— `grep "memory-tools" src/runtime/tools/` 零命中,唯一消费者 `memory-tools.ts` 自 v0.8 P2 §11.6 从 ALL_TOOLS 取消注册,生产环境零写入者(唯一写入是 v0.7 `memory.json` 一次性迁移 db-migration.ts:586)。MemoryNodeStore 才真活(compression-hooks.ts:153 wiki 写失败时回退写它 + `/api/memory-nodes` REST)。所以正确叙事不是"两套并存"而是"一套僵尸 + 一套 legacy 但活",已写进 12-glossary + 顺手修了同文件 first-writer-wins 旧术语 + mermaid 错误边)
- [ ] **[new, #21]** **跨文档扫描"两套并行 memory 表"叙事是否还有 v0.7 残留**:**MemoryStore 是否真删(僵尸清理)** —— #21 源码核对确认 `MemoryStore`(`memory_entities`/`memory_relations`)+ `memory-tools.ts`(memoryReadTool/memoryWriteTool)是**僵尸**(构造但零运行时写入者),但**文档/源码都还留着**。完整清理顺序已写进 12-glossary 的 MemoryStore 条目:① 删 `src/runtime/mcp-tools/memory-tools.ts`(零 importer,删了不影响 ALL_TOOLS);② 删 `src/server/memory-store.ts` + 其 `init()` 自建的两张表(若用户从未跑过 v0.7 迁移,表本来就空);③ 删 `src/server/db-migration.ts:586` 的 `memory.migrateFromJson()` 调用 + `memory.json` 解析分支(向后兼容就到此为止);④ 删 `src/server/session-db.ts:53,70,86` 的 MemoryStore 字段 + eager new + `getMemoryStore()` getter;⑤ 删 `src/runtime/session-store-interface.ts:58` 的接口方法;⑥ 文档侧 05 §2.2 / 06 §2.7 / 06 §4 表 / 10-tech-debt / 09-extension-points 同步去掉 MemoryStore 行(改记"v0.9 删除"或类似)。**注**:这是动 src/ 的工作,**不归 doc-loop 干**(章程硬约束"不动源码"),只是记在这里作为给用户的工程提案。需用户认可才执行。优先级低(留着不影响运行,但增加维护噪音 + 误导读者以为是活跃系统)。
- [x] **[new, #21]** **跨文档扫描"两套并行 memory 表"叙事是否还有 v0.7 残留**:扫描 `MemoryStore` / `memory-store` 在 **docs/basic/** 与 **docs/visualization/code-graph-data.json** 的叙事是否也停在 v0.7(12-glossary #21 修了,但 basic/ 与 code-graph 没扫)。code-graph-data.json 把 `memory-store.ts` 作为合法节点列出是 OK 的(文件确实存在),但描述可能没标"僵尸"。优先级低。 —— 见 #22(code-graph 数据层两个数据源 JSON + HTML 内联 DATA 都补 dead:true/supersededBy + 改描述;basic/ 已核对零错叙事,file-structure.md:97 memory-tools 行已是 v0.8 "legacy 未注册,已退役" 正确叙事,:122 memory-store 行"legacy 实体-关系图谱 memory"措辞略弱但不算错,不动)

## Progress log(最新在上)

#25 | 2026-06-27 | 文档批次(src 同步 open questions,9 commits 11 文件): code-graph agent-tool→agent-registry 改名 / requiresConfirmation 6 文档清零(04 08 09 10 11 12) / 09+10 #7 v0.7 残留修复 / 08 自愈退避+uncaughtException exit 改"已实现"(master 843e28a) / 11+07+backend-structure LOCAL 三分类(141 代理+7 invoke+7 receive) / 07 R 表三数字(141/102/20) / 11 quadrant v0.8 重评(DX↑) / 02 §4.1 三类→四类 | 9bfadad..9fa6950 | 文档批次汇总(非循环 cron 触发,用户驱动)
<!-- 每次触发追加一行: #N | <time> | <做了什么> | files | <commit> -->
- #24 | 2026-06-27 07:20 | 修 `docs/arch/11-quality-attributes.md` 全文一串 v0.7 残留事实错误(此前 #1~#23 全在做 02~08 + 12-glossary + basic + code-graph,**11 一直没人扫过**,质量属性文档对维护者又是高频参考)。**问题背景**:打开 11 一核就发现 5+ 处与 v0.8 实际不符的错,且与 #21/#4/#5/#18/#3/#17/#19 在其他文档建立的 v0.8 叙事**直接矛盾**。**逐节修复**:① **§2.0 仪表盘 + §2.1 延迟表**:删 "Memory 召回 100ms / FTS5 本地查询" 行,改为 "Wiki 检索 200ms" 行 —— MemoryRecall/MemoryNote 工具 v0.8 P2 §11.6 从 ALL_TOOLS 取消注册(`tools/index.ts:79-83` 注释明示),MemoryStore 是僵尸(#21 已确认),主线知识检索走 WikiStore + Wiki action 工具;顶部加 v0.8 更正 callout 解释为何这一行从 Memory 变 Wiki + 链 12-glossary + 06 §2.7;② **§3.4 优化机会**:WAL 行号补 kb-db + 加 "v0.8 引入 14 张工作流域表 + 9 个独立 new 的 store —— 单 sessions.db 写锁仍是全局串行点,但工作流域写入分散在不同 store/路由,热点表竞争压力小于 v0.7";③ **§5.1 当前保证**:**三处 stale 行号全修** —— "backend-spawn.ts:84-88" → ":117-130"(#4 已确认自愈逻辑在 117-130 不是 84-88)+ 加 "无重启计数/无退避" 警告(链 open questions)/ "main/ipc-proxy.ts:235-238" → ":380-382"(WS 重连实际在 380-382)/ **删 "recovery.ts 启动时扫描" 错文件引用** —— recovery.ts 文件不存在(`ls` 零命中),恢复路径在 `server/agent-service.ts:729-775`,turn_state 由 `durable-hooks.ts` 写入;④ **§5.2 重试逻辑精确化**:"重试 3 次后失败" → "`agent-loop.ts:427-454` 仅对 transient 错误重试,最多 `MAX_RETRIES=3`(`agent-utils.ts:29`),指数退避 base 1000ms";⑤ **§6.1 加工具成本**:补 "v0.8 后 buildTool(04 §2.0)自动注入 PreToolUse 阻断 hook + rateLimiter + recordToolUsage 遥测 + truncateResult,新工具无需手写这些横切关注点" + 加 CONDITIONAL_TOOLS 门控步 + 末尾标 "25 entries / 9 categories";⑥ **§6.4 加 IPC 通道成本**:重写为 ROUTE_MAP 派生工作流 + 标 LOCAL 通道实际清单(7 个:window×3 + dialog + webfetch:login + templates:github-preview/import-github,精确核对自 `node` 脚本算 R 表与 preload invoke 差集 = 7 个 LOCAL invoke 通道)—— 推翻了 #3/#17/#19 反复引用的 "17 LOCAL" 数字(那个数字把 receive-only event 通道也算了进来,11 这里精确分类 invoke 7 + receive 若干);⑦ **§6.5 加表成本**:重写为 v0.8 工作流域表走"批 A 阶段 2 显式 DDL"(不是简单的"加列")+ 标 ToolConfigStore/ToolUsageStore 手写 SQL 例外 + 标 server/index.ts:148-171 独立 new(链 05 §4.0.3 + 02 §4.1.1)+ 加前端 store 订阅 data:changed 步;⑧ **§7.1 规模假设**:"Memory nodes ~500" 加 "(v0.8 后写入路径仅会话压缩 wiki 写失败时回退,主线知识走 project_wiki 磁盘镜像树)" + 加 "项目(v0.8 工作流域)" 行;⑨ **§7.2 容量天花板**:"Memory nodes...FTS5 OK,更新 trigger 慢" → "Wiki/Memory nodes...FTS5 同步**没有 DB trigger**,由 memory-node-store.ts:128/131/134 的 prepared statement 在 store 方法里手动 upsert/delete(详见 05 §2.5 + 06 §2.7)—— 高频写时同步成本在 store 层不在 DB 层"(#18 已确认 TRIGGER 零命中,11 一直没改);KB chunks 行补 "knowledge.db 独立库,余弦相似度";LLM 并发行加 "provider-concurrency-manager.ts clamp 1-10";⑩ **§8.1 安全边界**(最严重一处):"contextBridge 限定到约 140 个代理通道 + 5 个本地通道" → "141 个 HTTP 代理通道(R 表项数,精确核对)+ 7 个 LOCAL invoke 通道(精确列)+ 若干 receive-only event 通道"+ "默认不限制 workspace(restrictToWorkspace = false 默认)" → "v0.8 后由 ctx.readScope === 'workspace' 控制(file-read/glob/grep 三处),不是简单的默认 false 布尔" + 加 "data:changed 走独立 IPC 通道" 一行(链 07 §2.3.1);⑪ **§8.2 风险评估**:Shell 行 "黑名单 + requiresConfirmation" → "黑名单 + PreToolUse 阻断 hook(v0.8 buildTool 内置,04 §2.0)" + 文件路径行补 "v0.8 已部分落地:readScope=workspace 门控";⑫ **§8.2 + §8.3 requiresConfirmation 退役标注**:删 "D-009 requiresConfirmation 接通" 修复项,加 ⚠️ callout 解释为何下线(meta.requiresConfirmation 字段贯穿但 agent-loop.ts 不读,是死字段,见 open questions #5 + 04 §12.2)。**未动**:§1 优先级排序(主观)/ §2.2 关键路径拆解(数值大致对)/ §3.1/§3.2/§3.3 并发模型(核对仍准)/ §4 一致性(核对仍准,db.transaction 仍大量使用)/ §6.2 加 Provider(provider-factory.ts 仍 case 分发)/ §6.3 加 Hook(4 小时估算主观)/ §6.6 跨层重构表(主观)/ §9 quadrantChart(主观评估,新加 backlog #24 让下次重评)/ §4.3 数据丢失表(WebFetch 24h 过期核对仍准)。源码逐行核对:`tools/index.ts:70-130,79-83`(ALL_TOOLS 25 entries + MemoryRecall 注释确认)/ `backend-spawn.ts:54,117-130`(_shuttingDown + exit handler + restart 确认,#4 已核)/ `agent-service.ts:729-775`(恢复路径确认)/ `ls src/main/recovery.ts`(零命中确认文件不存在)/ `durable-hooks.ts:42-89`(turn_state durable handler 确认)/ `ipc-proxy.ts:380-382`(WS reconnect 确认)/ `node R 表 vs preload invoke 差集脚本`(7 个 LOCAL invoke 通道精确确认)/ `ipc-proxy.ts:282`(app:ready 在 registerProxyHandlers 内单独 ipcMain.handle 确认)/ `agent-loop.ts:427-454` + `agent-utils.ts:29`(MAX_RETRIES=3 确认)/ `memory-node-store.ts:128,131,134`(三处 FTS prepared statement 确认)+ `grep TRIGGER memory-node-store.ts`(零命中,#18 已核)/ `provider-concurrency-manager.ts:21,73`(clamp 1-10 确认)/ `fetch-tools.ts:446-447`(24h 过期确认)/ `file-read.ts:101` + `glob.ts:91` + `grep.ts:225`(readScope=workspace 门控确认)/ `ipcMain.handle` 全 src/main 扫描(7 个 LOCAL 确认:window×3 + dialog + webfetch:login + templates×2) | docs/arch/11-quality-attributes.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #23 | 2026-06-27 06:50 | 新增 `docs/visualization/data-sync-flow.html`(种子 backlog 第 11 项的前半 —— data-change-hub 数据流可视化;wiki 懒加载树演示未做,逻辑完全不同,留独立项)。**问题背景**:种子 backlog 列了"新增可视化页:数据流(data-change-hub 推送)、wiki 懒加载树演示"一项,前 22 次触发全在做 arch/basic/code-graph 的 stale 内容修复与一致性,可视化这一高价值类(章程"按价值排序"第 4 优先级)一直没动。本次挑前半做 —— 数据流的 5 段路径(SqliteStore 写原语 emit → 白名单 gate → coalesce/flush → WS→IPC 桥 → 各 store 自订阅)有明确的源码锚点(每段都有 file:line)、有清晰的 v0.8 vs v0.7 取舍叙事(取代"每域一条通道/整表 refetch"),且 #11 在 07 §3.7 已用 mermaid 画过双路径拓扑但只是静态图,可交互版能让读者**逐 emit 演练** coalesce / burst→refetchAll / 过滤列表不在等微妙路径。**这是 zero-core 第 7 个 visualization HTML**(前 6 个:agent-loop-anatomy / agent-loop-architecture / code-graph / compression-flow / tool-playground + code-graph-data.json),沿用同样的暗色主题 + 内联 CSS/JS + 零外部 CDN 风格。**页面结构**:① **顶部控制条** —— 6 情景下拉(单次写 / 同 tick 多写合并 / burst 太大 → 全量 refetch / 非白名单表静默丢 / delete 不带 record / 过滤列表新 id 不在)+ 单步/自动播放/重置 + step 计数;② **主图** —— 5 列泳道(写源 SqliteStore / 白名单 gate / hub flush / WS→IPC 桥 / store 自订阅),用 SVG 在泳道容器内画曲线边,随 stepIdx 点亮"hot"路径(非 hot 的边半透明);每个节点带源码 file:line 标注(emit→sqlite-store.ts:265,311,319 / wl→hub:86 / coalesce→hub:87-94 / flush→hub:63-78 / broadcast→index.ts:268 / ipc→ipc-proxy:369 / 5 个 store 自订阅点 agent:136 project:90 cron:127 requirement:184 wiki:198);点任一节点 → 详情卡展开该段源码 + 设计取舍;③ **演练区双栏** —— 左栏 tick 序列(每 tick 一行,emit 用 c/u/d 彩色 chip 标 op,同 tick 多写显式列出,非白名单表显式标 `drop`,flush 行标紫色高亮)+ 右栏 5 个 store 的接收态(每 store 显式列收到的 changes,带 chip + id 截断);④ **详情卡** —— 默认显示当前情景说明,点主图节点切到节点详情。**情景演练的真实源码映射**:单次写 = management REST 改 agent prompt;coalesce = archivist 一个 tick 扫 200 wiki 节点(同 id 后写覆盖前写,但因 id 各不同 changes 数组很大,wiki 树形 drop+refresh);burst = migrateFromJson import 几百 agent(helper 在第一个 patch false 就 refetchAll + return,后续不再处理,实际只 refetch 一次 —— 这是 source code 的微妙点,文档 + 演练都标了);drop = 流式 LLM 每 chunk 一条 messages 写入(几十 Hz)在 gate 处被吞;delete = emit op=delete 不带 record,store patch 收到 record=null 时 filter 掉 id;filter = 项目 A 视图后端为项目 B 创建 requirement,patch 返 false 触发 refetchAll(虽然还是过滤掉,但确保 filter 一致)。**关键设计取舍写进详情卡**:① server `index.ts:268` 只转发 `collection` 不转发 `changes` 数组,但 preload 类型 + ipc-proxy:372 引用 `changes` —— 实际运行时 undefined,data-sync.ts:56 用 `?? []` 兜底(这是 #3 在 07 §2.6 已记的 v0.8 不对称点,演练页 + 节点详情再次强调);② wiki 是唯一用 subscribeDataChange(全量 refetch)而非 subscribeListDataChange(增量 patch)的 store,因树形无法简单 patch;③ data:changed 走独立通道不污染 agent:event 流(双路径并行的关键);④ hub gate 必须在最底层(SqliteStore)就近做,否则高频表刷爆 IPC。**未做**:wiki 懒加载树演示(种子 backlog 第 11 项后半,独立项);**未动其他 visualization HTML**(新增文件,零风险)。**验证**:`node -e` 提取 `<script>` body + `new Function(js)` 解析 OK(JS 语法正确,22147 字符)/ `<div>` 开闭标签平衡 62/62 / 文件 36081 字节。源码逐行核对:`data-change-hub.ts:32-100`(UI_COLLECTIONS 5 个 + emitDataChange:85-94 + flush:63-78 + onDataChange:97-100)/ `data-sync.ts:30-70`(subscribeDataChange:30-35 全量 / subscribeListDataChange:49-70 patch+refetchAll 兜底 + delete 忽略返回值 + `e.changes ?? []`)/ `server/index.ts:262-271`(onDataChange→broadcast 只转发 collection + AgentStore.onChange 保留备注)/ `ipc-proxy.ts:355-378`(WS→IPC 桥 + data:changed 独立通道 vs agent:event)/ `preload/index.ts:100-104`(onDataChanged 类型带 changes 但实际可空)/ `sqlite-store.ts:265,311,319`(三处 emitDataChange 调用确认)/ `agent-store.ts:136-143`(非过滤 patch 返 true)/ `requirement-store.ts:184-198`(过滤列表返 false 触发 refetchAll)/ `wiki-store.ts:198-206`(树形 drop+refresh root) | docs/visualization/data-sync-flow.html(新增), docs/arch/07-renderer-and-ipc.md(§2.3.1 加交叉链接), docs/.docloop/TRACKER.md | (本次 commit) |
- #22 | 2026-06-27 06:25 | 在 code-graph 数据层把 `memory-store.ts` 与 `memory-tools.ts` 两个**僵尸节点**显式标 dead(backlog #22,#21 在 12-glossary 把 MemoryStore 钉成僵尸后留下的 code-graph 收尾)。**问题背景**:#21 源码核对已确认 `MemoryStore`(`memory_entities`/`memory_relations`)是**僵尸** —— 构造但零运行时写入者,唯一消费者 `memory-tools.ts`(memoryReadTool/memoryWriteTool)自 v0.8 P2 §11.6 从 `tools/index.ts:84` ALL_TOOLS 取消注册(注释明示 "MemoryRecall / MemoryNote tools removed — memory is now a wiki per-agent subtree")。但 code-graph 的两个数据源(外置 `code-graph-data.json` + `code-graph.html` 内联 DATA)都把这两个文件当合法活节点列出,描述还是 v0.7 "agent 记忆持久化" / "知识图谱记忆工具" 的措辞,读者打开 code-graph 会被误导。**这是 #15 已经为 `subagent-delegation.ts` 落地的死标记模式的直接复用** —— 渲染层(`.dead-badge` / `.edge-list li.dead` 灰显+删除线 / `showFileDetail` DEAD CODE 徽章 / `showDirDetail` 文件列表灰显 / `renderFileBrowse` Calls 面板灰显)早已通过 `fi.dead===true` 分支处理任意 dead 节点,**本次只动数据层**,渲染层零改动即自动生效。改:① **数据层 JSON** `src/server/memory-store.ts` 节点加 `"dead": true` + `"supersededBy": "src/server/wiki-node-store.ts"`(v0.8 主线知识系统已迁到 WikiStore,每 agent 一个 wiki 子树)+ 描述整段重写为"⚠️ 僵尸(zombie):零运行时写入者 / 唯一写入路径是 v0.7 memory.json 一次性迁移(db-migration.ts:586)/ SessionDB 仍 eager new(:70)但只为了 getDb() 链路稳定 / 详见 12-glossary + 06 §2.7 矩阵 / 不要与 MemoryNodeStore 混淆(兄弟而非父子,表结构无关联数据不互通)";② **数据层 JSON** `src/runtime/mcp-tools/memory-tools.ts` 节点加 `"dead": true` + `"supersededBy": "src/runtime/mcp-tools/platform-tools.ts"` + 描述整段重写为"⚠️ 僵尸(zombie):`grep -r 'from .*memory-tools' src/` 零 importer / v0.8 P2 §11.6 注释明示 MemoryRecall/MemoryNote 工具移除 / 文件没删所以静态 import 图还有 tool-factory.ts 边但运行时永不进工具清单 / 是 memory-store.ts 的唯一消费者,二者构成完整死链 / 详见 12-glossary";③ **数据层 HTML 内联 DATA**(815KB 单行,用 node 脚本 brace-match 提取 + JSON.parse 重写 + JSON.stringify 回填,避免 Edit 工具读不进 27k 行级单行)对两个节点同样改动。**验证**:`JSON.parse(code-graph-data.json)` OK 215 files / HTML 内联 DATA brace-match + `JSON.parse` OK 215 files(与外置一致)/ `<script>` 内容用 `new Function(body)` 解析 OK(JS 语法正确)/ 全量 dead 节点核对 = 3 个(`subagent-delegation.ts` + `memory-store.ts` + `memory-tools.ts`,前一个是 #15 加的,后两个本次新加)。**supersededBy 选择说明**:memory-store → wiki-node-store(v0.8 主线知识系统 wiki 每子树);memory-tools → platform-tools(同目录其他 mcp-tools,纯位置参考而非功能取代 —— memory-tools 没有真正的功能继任者,因为整个 memory-as-tool 概念在 v0.8 被 wiki 取代了,但 supersededBy 字段需要一个目标节点存在,挑了同目录邻居避免悬空)。**未动**:code-graph 渲染层 CSS / JS(自动生效);docs/basic/(扫描确认 file-structure.md:97 memory-tools 行已是正确 v0.8 "legacy 未注册,已退役" 叙事 / :122 memory-store 行"legacy 实体-关系图谱 memory" 措辞略弱但不算错 / 其他 basic 文档零 MemoryStore 引用);02/05/10 arch md(都是结构事实提及 MemoryStore 仍 eager new 在 SessionDB:70,这是真实状态不算 v0.7 残留,且这些文档的目标读者是改架构的人不是死代码雷达用户)。**遗留**:`src/server/` 是否还有其他零 importer 死代码候选仍 backlog(open,需用户认可"死代码灰显作为 code-graph 长期产品定位"后才值得写批量扫描脚本)。源码逐行核对:`grep -rn "from .*memory-tools" src/`(零命中,确认僵尸)/ `src/runtime/tools/index.ts:86`(注释 "MemoryRecall / MemoryNote tools removed — memory is now a wiki per-agent subtree" 确认)/ `grep -rn "from .*subagent-delegation" src/`(零命中,#15 已确认,本次再次复核渲染层 dead 节点清单时附带验证仍准)/ `grep "memory-store\|MemoryStore" docs/basic/`(仅 file-structure.md 两行,扫描确认无错叙事)/ `grep -c '"path":"src/server/memory-store.ts"' docs/visualization/code-graph.html`(= 1 确认 HTML 内联 DATA 唯一节点) | docs/visualization/code-graph-data.json, docs/visualization/code-graph.html, docs/.docloop/TRACKER.md | (本次 commit) |
- #21 | 2026-06-27 05:50 | 修 12-glossary.md 的三处事实错误 + 同文件顺手修 first-writer-wins 旧术语(backlog #21,跨文档一致性扫描)。**问题背景**:#20 在 progress log 里把 `MemoryStore` 与 `MemoryNodeStore` 描述成"两个并行系统,两套都活,数据不互通",并记 backlog #21 让扫其他 arch md 是否有"`memory_entities` 已废"或"MemoryStore 已删"的 v0.7 残留错叙事。**扫描结果**:02 / 06 / 08 都没有这种错叙事(02 §4.1 A 表与 #10 已是正确"3 eager 内核 store"叙事 / 06 §2.7 对比矩阵 + §4 legacy 表早已准 / 08 只在 §2.4 提一句 memory-hooks 已废);真正需要修的只有 **12-glossary.md** 一篇。**但核对源码时发现 #20 自己的措辞也不准** —— MemoryStore **不是"活"**,是**僵尸**(zombie):① `grep "memory-tools" src/runtime/tools/` 零命中,`memory-tools.ts` 里的 `memoryReadTool`/`memoryWriteTool` 是它唯一消费者,自 v0.8 P2 §11.6 从 `tools/index.ts:84` ALL_TOOLS **取消注册**(代码注释明示 "MemoryRecall / MemoryNote tools removed — memory is now a wiki per-agent subtree");② `grep "from.*memory-tools" src/` 零命中,文件无 importer;③ `getMemoryStore()` 的运行时调用方只有 `memory-tools.ts` 自己(`memory-tools.ts:26/48/105`)+ `db-migration.ts:586`(v0.7 `memory.json` 一次性迁移,启动时跑一次就不再触发)。**结论**:MemoryStore 的唯一运行时写入路径是 v0.7 `memory.json` 迁移(若用户从未有过 v0.7 数据,两表永远空);生产环境 Agent 永远不写它。所以正确叙事是 **"MemoryStore 僵尸(构造但零运行时写入)+ MemoryNodeStore legacy 但活(compression 回退 + REST)"**,而不是 #20 说的"两套都活"。MemoryNodeStore 的活跃性已逐行核对:`compression-hooks.ts:153` 在 wiki 写失败时 `config.db.getMemoryNodeStore().upsertNodes(...)` + `server/index.ts:487` `app.use("/api/memory-nodes", createMemoryNodeRouter(sessionDB.getMemoryNodeStore()))`。**两者关系**:兄弟而非父子 —— 各自 eager new(`session-db.ts:70` vs `:71`)、各自 `init()` 自建表、表结构无关联、数据不互通。改(全在 12-glossary.md 一个文件):① **mermaid 知识子图**:删 `K4 --> K5` 这条**假依赖边**(二者无任何引用关系),改 `K4[MemoryNodeStore]`+`K5[MemoryStore 旧]` 两个无标签节点为 `K4["MemoryNodeStore<br/>(压缩回退)"]`+`K5["MemoryStore<br/>(僵尸)"]`(节点标签直接暴露状态)+ 补 `K6["WikiStore<br/>(主线)"]` 节点 + 把 `K1 --> K4` 改为 `K1 --> K6`(ToolRegistry 实际依赖 WikiStore 不是 MemoryNodeStore,v0.8 主线已是 wiki)+ 用虚线 `K4 -. 兄弟并存,数据不互通 .-> K5` 显式标"兄弟而非父子"(保留两节点的视觉邻近,但用虚线 + 文字标签强调无依赖);② **MemoryStore 术语条目** 整段重写 —— 加 ⚠️ 僵尸标识 + 列两张表 + 明示"构造时 eager new + v0.7 memory.json 迁移是唯一写入路径 / 运行时零写入者" + 给出删除清理顺序(删 memory-tools.ts → 删类 + 两表 + 迁移分支 + SessionDB getter)+ 强调"不要与 MemoryNodeStore 混为一谈,二者数据不互通、互不引用、互不依赖"+ 交叉链 05 §2.2 + 06 §2.7;③ **MemoryNodeStore 术语条目** 整段重写 —— 列四张表(含 FTS5)+ 强调"与 MemoryStore 是兄弟而非父子,各自 eager new、各自 init()、表结构无关联"+ 明示活跃调用方(`compression-hooks.ts:153` 回退 + `/api/memory-nodes` REST)+ 标"legacy 但活,与僵尸的 MemoryStore 对照";④ **同文件顺手修 first-writer-wins 旧术语**(也是跨文档一致性,#1 早已在 02/03/08 修过但 12-glossary 一直漏):`first-writer-wins:hook 触发语义,第一个返回非空结果的 handler 决定后续行为` → 改为 ⚠️ 旧叙事已废块,明示源码实际是 last-writer-wins merge + blocked 短路(链 hook-registry.ts:78-91 + 03 §3 + 02 §3.2),保留词条仅为反向澄清。**未动**:02 / 06 / 08(扫描确认无错叙事)/ 05(#20 已正确列出 MemoryStore vs MemoryNodeStore 两套)。源码逐行核对:`grep "memory-tools" src/runtime/tools/`(零命中确认 ALL_TOOLS 不注册)/ `grep "from.*memory-tools" src/`(零命中确认文件无 importer)/ `src/runtime/tools/index.ts:84-87`(注释 "MemoryRecall / MemoryNote tools removed — memory is now a wiki per-agent subtree" 确认)/ `grep -rn "memoryReadTool\|memoryWriteTool" src/ tests/`(仅 memory-tools.ts 自身 + memory-store.ts:15 注释)/ `grep "getMemoryStore\(\)" src/`(memory-tools.ts × 3 + db-migration.ts:586 迁移 + session-store-interface.ts 类型声明,无其他运行时调用)/ `grep "getMemoryNodeStore\(\)" src/`(compression-hooks.ts:153 + index.ts:487 + session-store-interface.ts 类型声明,确认 MemoryNodeStore 活跃)/ `memory-store.ts:97,104`(memory_entities/memory_relations DDL 确认)/ `session-db.ts:70-71`(MemoryStore + MemoryNodeStore 都 eager new 确认,各自独立)/ `grep "memory-hooks" src/`(零命中确认 §11.6 退役) | docs/arch/12-glossary.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #20 | 2026-06-27 05:25 | 修 05-persistence.md §2 / §2.2 / §2.2b / §3 / §4.0.3 / §11.1 / §11.2 一串"表计数"连环事实错误(backlog #41,#18 修 §2.5 FTS5 时记下的"逐字核 SQL 块"后续)。**问题背景**:打开 05 准备逐字核 SQL 块,但 `grep -rEn "CREATE TABLE" src/` 一跑就发现**表总数与文档叙事全面对不上**,根因是"30 张表"叙事在 #7 / #8 / #16 / #18 多轮迭代中反复加减但从未统一对账,各章节互相传染。三处主错:① **§2.2 业务实体表把 v0.8 §11.5 已 `DROP TABLE` 的 `agent_tools` 当活表列**(`db-migration.ts:616` 显式 `DROP TABLE IF EXISTS agent_tools` + `ls src/server/agent-tool-store.ts` 文件不存在),实际存活业务表只有 10 张;② **"9 张 v0.8 工作流域表"漏数 5 张**:`grep -oE 'db\.exec\(`CREATE TABLE IF NOT EXISTS [a-z_]+' db-migration.ts` 阶段 2 实际有 **14 个** `db.exec`(projects/project_wiki/wiki_scan_cursors/requirements×3/task_steps/crons/cron_runs/project_jobs/tool_configs/tool_usage/orchestrate_plans/orchestrate_manifests),漏掉的 5 张是 wiki_scan_cursors / requirement_status_history / requirement_messages / tool_configs / tool_usage;③ **"memory_* 4 张自建表"漏数 MemoryStore 的 2 张**:核对发现 `memory-store.ts` 是**活代码**(`grep "MemoryStore" src` 命中 `session-db.ts:70` eager new + `mcp-tools/memory-tools.ts:26` MCP 工具后端 + `db-migration.ts:33` import),它 `init()`(`memory-store.ts:97/104`)构造时自建 `memory_entities` + `memory_relations` 2 张表 —— 文档 §2.2 第 142-143 行其实列出来了,但"4 张" callout 把它们排除在外。这同时纠正了 #19 日志里把 `memory_entities`/`memory_relations` 笔误为"v0.7 名"的错(实际两套 memory 表都活着,是并行的两个系统:MemoryStore 服务 MCP memory-tools / MemoryNodeStore 服务会话压缩回退)。**根因**:#7 落地"30 张表"叙事时是凭印象分的组(11 + 9 + 4 + KV/FTS5/telemetry/cursor = 30,数字凑得齐),后续 #8 / #16 / #18 都没回头重算总数,所以"11 + 9 + 4"这三个子数各自都有 ±2~5 的误差,合起来正好凑成 30 但每个数都不真。改:① **§1 目录树 + §2 标题**:重写为正确的"≈33 张表 = 批 A db-migration 管理 24 张 [5 会话核心 + 5 旧业务实体 + 14 v0.8 工作流域] + 批 B 构造自建 9 张 [6 memory_* + kv_store + extraction_cursors + tool_telemetry]";② **§2 顶部"表计数的口径" callout 整段重写**:显式标"此前 30 张 + memory_* 4 张"三处都对不上(退役 agent_tools / 漏数 v0.8 工作流域表 / 漏数 MemoryStore 表),列正确口径 = 批 A(阶段 1 SessionDB 5 + 阶段 2 显式 DDL 14 + 阶段 3 SqliteStore 构造 5)= 24 张 / 批 B(kv_store + memory_* 6 + extraction_cursors + tool_telemetry)= 9 张;③ **§2.2 业务实体表**:`agent_tools` 行整行加删除线 + 改备注为"v0.8 §11.5 退役 + 链 08 §11.5 + 标 `db-migration.ts:616` DROP"(之前当活表);**§2.2 标题** "业务实体表" → "业务实体表(10 张存活 + 1 张已退役)";**memory_entities / memory_relations 行备注** 加"⚠️ 构造自建,见下"(之前漏标);**§2.2 末尾 callout 重写** "memory_* 4 张自建表" → "memory_* 6 张自建表(MemoryStore 2 张 + MemoryNodeStore 4 张)",并**新增"批 B 构造自建表全清单"段** 把 9 张全列出(kv_store / 6 memory_* / extraction_cursors / tool_telemetry),澄清 MemoryStore vs MemoryNodeStore 两套并存;④ **§2.2b 开头** "新增 9 张表" → "新增 14 张表" + 改"唯一例外 ToolConfigStore"为"两个例外 ToolConfigStore + ToolUsageStore"(后者 `tool-usage-store.ts:32` 注释为避免 SqliteStore 自愈 ALTER 加列动表结构也手写);**§2.2b tool_usage 行** 加"**手写**"标记;⑤ **§3** "v0.8 工作流域 9 张表全部复用 SqliteStore" → "14 张表中 12 张复用 SqliteStore,例外见 §2.2b 末";⑥ **§4.0.3** "9 张工作流域表(...)" → "**14 张工作流域表**" 列全 + 补一句"显式 new 的 Store 实例数是 9 个,但 RequirementStore 内部聚合 historyStore + messageStore 两个内部 SqliteStore → 9 个 store 实例映射到 14 张表"(消除"9 = 14"的数字歧义);⑦ **§4.2 第 682 行 + 第 701 行** "9 张 v0.8 表" → "14 张 v0.8 表";⑧ **§11.1** "v0.8 9 张工作流域表全部复用 SqliteStore 同一地基" → "14 张工作流域表中 12 张全部复用 SqliteStore(ToolConfigStore + ToolUsageStore 手写 SQL)";⑨ **§11.2** "v0.8 把表数翻倍(15→30)" → "≈15→≈33 张(口径见 §2 顶部块)";"9 张 v0.8 表的 DDL" → "14 张 v0.8 工作流域表的 DDL"。**未动**:§2.1 会话核心表 / §2.2b 14 行矩阵本身 / §2.3 KB chunks / §2.4 KV store / §2.5 FTS5 / §2.13 双 ER 图 / §4.0.1 / §4.0.2 / §4.0.4 / §4.2 5 阶段主体(这些章节的子计数本身已对,只是被父级"30 张表"叙事拖累,父级修了即可)。**新发现 + 已记录为新 backlog**:`MemoryStore` 与 `MemoryNodeStore` 是两个并行系统的事实,#19 日志曾把前者笔误为"v0.7 名",其他 arch md(02 / 06 / 08 / 12-glossary)可能也有同类错叙事,新加 backlog #21 跨文档扫。源码逐行核对:`grep -oE 'db\.exec\(`CREATE TABLE IF NOT EXISTS [a-z_]+' src/server/db-migration.ts | sort -u | wc -l`(14 张阶段 2 表确认)/ `grep -n "new SqliteStore" src/server/db-migration.ts`(5 张阶段 3 表确认:agents/providers/templates/mcp_servers/kb_entries)/ `grep -oE 'CREATE TABLE IF NOT EXISTS [a-z_]+' src/server/session-db.ts | sort -u`(5 张会话核心表确认)/ `grep "memory_entities\|memory_relations" src/server/db-migration.ts`(零命中确认 MemoryStore 自建)/ `memory-store.ts:97/104`(memory_entities/memory_relations DDL 确认)/ `memory-node-store.ts:140/154/163/181`(4 张 MemoryNodeStore 表确认)/ `key-value-store.ts:53`(kv_store 自建确认)/ `extraction-cursor-store.ts:77` + `telemetry-store.ts:97`(2 张 v0.8 M5 lazy store 自建确认)/ `ls src/server/agent-tool-store.ts`(文件不存在确认)/ `db-migration.ts:616`(DROP TABLE agent_tools 确认)/ `grep "MemoryStore" src`(确认是活代码,被 session-db.ts:70 eager new + memory-tools.ts:26 用)/ `session-db.ts:70-71`(MemoryStore + MemoryNodeStore 都 eager new 确认)/ `tool-usage-store.ts:29/32/126`(两个手写 SQL 注释确认 ToolConfigStore + ToolUsageStore 都手写)/ `kb-db.ts:46-51`(确认 kb_chunks 在独立 knowledge.db 不算入 sessions.db)/ 5 + 5 + 14 + 9 = 33 张总数核算(批 A 24 + 批 B 9 无重叠) | docs/arch/05-persistence.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #18 | 2026-06-27 04:20 | 修 05-persistence.md §1/§2/§2.2/§2.3/§2.5/§2.13/§9 一串互相传染的"数据库物理分布"事实错误(backlog #39 扩成连环修)。**问题背景**:backlog #39 只点了"memory_* 4 张表不进 db-migration"一处,但实际打开 05 一核就发现**5 处互相传染**的错:① §1 顶部 + 目录树 + §11.2 备份行 + §9.1 流程图都用 `db.sqlite` 作主库文件名 —— 实际 `session-db.ts:63` 写的是 `sessions.db`(`grep "db.sqlite" src/` 零命中,只在 docs/ 里出现,06 §2.7 已是正确的 `sessions.db`,05 自己内部就不一致);② §2.3 标题"独立 SQLite 文件?" + 正文"非也 —— KbDB 也使用同一个 db.sqlite" **直接错** —— `kb-db.ts:46-51` 在构造函数里 `new Database(join(ZERO_CORE_DIR, "knowledge.db"))` 开了独立连接,kb_chunks 根本不在 sessions.db;#16 在 06 §2.7 已经把这个事实系统化讲清,但 05 自己一直没改;③ §2.2 业务实体表里 `memory_nodes`/`memory_subjects`/`memory_edges` 当成正常业务表列出,但它们**不在 db-migration**(`grep "memory_nodes" db-migration.ts` 零命中),由 `MemoryNodeStore.init()`(`memory-node-store.ts:138-184`)构造时自建;④ §2.5 FTS5 虚拟表 DDL 写 `subject, type, content` —— 实际 DDL 只有 `subject` + `content` 两列(没有 `type`),且缺 `tokenize='unicode61'`;⑤ §2.5 同段写"带 INSERT / UPDATE / DELETE trigger 保持 FTS 索引同步" —— `grep "TRIGGER" memory-node-store.ts` 零命中,实际是 store 方法里**手动**调 prepared statement(`memory-node-store.ts:128/131/134` 的 SELECT MATCH/INSERT/DELETE 三个 stmt)同步 FTS,不是 DB 层 trigger。**根因**:这 5 处都是 v0.7 时期写的描述,后续 #2/#7/#16 在 06 和 05 §2.2b/§4 里把 v0.8 的新表讲清了,但 05 自己最早的 §1/§2.2/§2.3/§2.5/§2.13a 这几节一直没人回头核对,旧叙事和新叙事在同一篇文档里并存互相矛盾。改:① **§1 顶部 + 目录树**:把"所有用户数据都在 db.sqlite 一个文件里"改为"主要落在 sessions.db(主库)+ knowledge.db(向量库,只存 kb_chunks)+ wiki/(磁盘镜像树)+ 旧 JSON",目录树补 `sessions.db`(标"§2 全部表 + memory_* 4 张自建表")+ `knowledge.db`(标"KbDB 独立连接,只存 kb_chunks,§2.3")两行;② **§2 标题下加"30 张表口径"块**:显式说明"30 张表"严格指 db-migration.ts 管理的表,sessions.db 物理表数 > 30(再加 memory_* 4 张自建),且 kb_chunks 根本不在 sessions.db(§2.3);③ **§2.2 业务实体表**:memory_nodes/subjects/edges 三行备注加"⚠️ 构造自建,见下"标记,表下新增 callout 块讲清 4 张 memory_* 表不进 db-migration、改 schema 要去 memory-node-store.ts 的 init()、FTS 重建逻辑(`init()` 检测旧列结构 DROP 重建)只有 init() 有 db-migration 没有、链 06 §2.7;④ **§2.3 整节重写**:标题改为"KB chunks —— 独立 SQLite 文件 knowledge.db",顶部加 v0.8 更正块显式标旧版错(链 kb-db.ts:46-51 + 06 §2.7),加"为什么独立成库"叙事段(embedding BLOB 大 + 写入频繁会拖慢主库 WAL checkpoint + 备份膨胀),加"建表机制"说明 kb_chunks 不进 db-migration 由 KbDB.initSchema() 自建(`kb-db.ts:56-69`,与 memory_* 同类"store 自己管 schema"模式),DDL 改为真实的 `id/kb_id/file_path/chunk_index/content/embedding/token_count/created_at` + 两个索引(`idx_kb_chunks_kb`/`idx_kb_chunks_file`,旧文档漏),末尾加跨库关联说明 `kb_entries.id`(sessions.db)↔`kb_chunks.kb_id`(knowledge.db)是应用层 join SQL 层不能联表;⑤ **§2.5 FTS5 整节重写**:行号 `200-260` 改为正确的 `173-191`(init() 内 FTS 块),DDL 改为真实两列 + `tokenize='unicode61'` + 外部内容表模式(`content='memory_nodes'`),顶部加 v0.8 更正块标旧版"subject,type,content"+ "trigger" 两处都不准,把"trigger 同步"叙事改为"store 方法里手动 prepared statement 同步"(列三个 stmt 行号 + 解释 upsertNode/deleteNode 双保险路径,即使外部内容表本应自动同步也手动一次)+ 加"改 FTS schema 要复用 init() 的 drop-and-recreate 模式不要 ALTER"提示;⑥ **§2.13 表关系图引言** 加"读图注意(物理分布)"段:KB_CHUNKS 节点不在 sessions.db 而在 knowledge.db(那条边是跨库应用层 join),memory_* 节点虽然在 sessions.db 但不进 db-migration,这两类例外已在对应小节标出此处不画物理库边界以免图过载;⑦ **§9 备份与一致性**:第一行从"SQLite 是单文件,备份 db.sqlite"改为"数据分散在 sessions.db + knowledge.db + wiki/ + 旧 JSON,备份整个 ~/.zero-core/ 最稳妥(只复制 sessions.db 会丢 wiki 正文 + kb 向量)";⑧ **§9.1 流程图** `DB[(SQLite db.sqlite)]` 改为 `DB[(SQLite sessions.db)]`。**未动**:#7 已重写的 §2.2b v0.8 工作流域表矩阵 + §4.2 迁移机制(那两节本来就用正确的 sessions.db 叙事);02-module-structure.md / file-structure.md 若也提到 db.sqlite 应一并扫(backlog 新加一条)。源码逐行核对:`session-db.ts:63`(`join(ZERO_CORE_DIR, "sessions.db")` 确认) / `kb-db.ts:46-51`(constructor `new Database(join(ZERO_CORE_DIR, "knowledge.db"))` + WAL pragma 确认) / `kb-db.ts:56-69`(initSchema CREATE kb_chunks + 两索引确认) / `grep "db.sqlite" src/`(零命中确认) / `grep "memory_nodes" src/server/db-migration.ts`(零命中确认) / `memory-node-store.ts:138-184`(init() CREATE 4 表 + FTS DROP-recreate 块确认) / `memory-node-store.ts:128,131,134`(三个 FTS prepared statement 确认) / `grep "TRIGGER" src/server/memory-node-store.ts`(零命中确认"trigger 同步"是错的) / `memory-node-store.ts:175-188`(实际 FTS DDL `subject/content/content='memory_nodes'/content_rowid/tokenize='unicode61'` 确认) / `grep "db.sqlite" docs/arch/`(确认 05 是唯一残留,06 §2.7 已正确用 sessions.db) | docs/arch/05-persistence.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #17 | 2026-06-27 03:50 | 把 `docs/basic/` 三篇速查表(tech-stack / backend-structure / app-flow)与 v0.8 实际结构对齐(backlog #42,#9 修 file-structure.md 时记下的"其他几篇也可能 stale"后续)。**问题背景**:这三篇定位是"给新人看的扁平速查表",但所有计数 / 命令 / 模块名都停在 v0.7,与 #1~#16 在 arch md 里建立的 v0.8 叙事**直接矛盾**,读者读完 arch 再回头看 basic 会被误导。逐篇核对发现的具体 stale 点:**tech-stack.md**:① "OpenPrd 工具链"整节(6 条 openprd 命令表)与事实不符 —— 用户 memory `project-no-openprd-flow` 明示"当前不用 openprd 流程",`grep "openprd" package.json`(零命中,只在 .claude/settings.local.json 一处),package.json 的 scripts 一个 openprd 都没有;② 工具链表里 tsx 的用例 `scripts/check-handler-modules.ts` 是**幽灵文件**(`ls scripts/` 实际只有 5 个文件,无 check-handler-modules);③ 构建 scripts 表 8 行有 2 行不存在(`serve` / `check:handlers` 都不在 package.json),`build` 实际执行 `npm run typecheck && electron-vite build`(不是"lib + electron-vite"),且漏了 6 条真实脚本(typecheck / build:lib / preview / test:unit:watch / build:codegraph)。**backend-structure.md**:① 路由表漏 6 个 v0.8 路由(`/api/crons` / `/api/orchestrate` / `/api/pm` / `/api/wiki` / `/api/archivist` + `/api/agent-tools` 退役标注);② 服务边界表 "Feature Hooks | 4 个 handler(compression/memory/RAG/index)" —— memory-hooks 已删,实际 7 个;③ "Hook 系统 | 29 个生命周期事件" —— 实际 30 个(20 活跃 + 10 零触发);④ "模板管理 | 12 内置" —— 实际 16(12 基础 + 4 v0.8 领域专家)+ 1 工作流角色 zero;⑤ IPC 接入面段写 "typed-ipc.ts + registerCrud 注册 17 个 CRUD 通道" —— `typed-ipc.ts` 已不存在,v0.8 走 `ipc-proxy.ts` 的 `registerProxyHandlers` + ROUTE_MAP 派生(07 §2.5 已是 v0.8 视角,basic 没跟上);⑥ 数据流段 "PostTurnComplete → memory-hooks → memory_nodes" —— memory-hooks 已删;⑦ 数据存储表列 `AgentToolStore` —— v0.8 §11.5 退役,文件 `agent-tool-store.ts` 已删(`ls` 零命中,`agent-router.ts:70` 注释明示 no rows to cascade);⑧ IPC 接入面与数据存储表里"12 内置 + AgentToolStore + 17 通道"三处互相传染。**app-flow.md**:① 启动流程 "feature hooks(compression/memory/RAG)" —— memory-hooks 已删,实际 7 个 handler;② 启动时 "agentToolStore.cleanupOrphans() 清理孤儿 agent-tool" —— 函数与 store 都已下线,v0.8 启动清理的是 project_wiki 孤儿子树(`server/index.ts:360-364`);③ "PostTurnComplete(compression/memory extraction)" —— memory extraction 已删,改 extraction-hooks(内容记忆/工具遥测双提取者);④ Agent 删除流程 "afterDelete 级联删 agent-tool 条目" 整段已废。改:**tech-stack.md**:① 删 OpenPrd 整节,改为"工程常规门禁(替代旧 OpenPrd 流程)" —— 6 行真实 npm 命令表 + 解释 standards/quality/run verify 无对应 npm 脚本已下线 + 链 memory feedback-build-verification 解释为何 typecheck+build:lib 必跑;② 工具链表 tsx 用例改为 `build-codegraph.ts` / `test-tool-output.ts`;③ 构建 scripts 表从 7 行重写为 11 行(全部对齐 package.json 实际值,每行加"实际执行"列 + 标 build 内部含 typecheck 不是 lib+electron-vite + 标 e2e 含 build + 加 build:codegraph 27k 行级别说明)+ 加注解释 serve / check:handlers 两条过时项已删(scripts 都没定义 + scripts/check-handler-modules.ts 文件不存在)。**backend-structure.md**:① 路由表从 21 行重写为 25 行 —— 删 `/api/agent-tools`(标 v0.8 §11.5 退役)+ 补 6 个 v0.8 路由(crons/orchestrate/pm/wiki/archivist + templates 改 16 内置说明 + project_wiki 标 06 §2.5)+ agents 路由行标注 agent-tools 退役;② 服务边界表 Feature Hooks 行 4→7 个 handler(列全 + 标 memory-hooks v0.8 P2 §11.6 删除);③ Hook 系统 29→30 个事件(标 20 活跃 + 10 零触发,链 08 §2.4);④ 模板管理 12→16 内置 + 1 工作流角色 zero 单独管理说明;⑤ 数据流段 memory-hooks→extraction-hooks(标 v0.8 M5 双提取者 + 解释 memory_nodes 表仍存在但仅压缩回退路径写入);⑥ IPC 接入面段重写 —— 删 typed-ipc/registerCrud/17 通道叙事,改为 ipc-proxy.ts registerProxyHandlers + ROUTE_MAP 派生 + 155 preload 方法 / 120+ 代理 / 17 LOCAL / 3 INVOKE 三组例外(链 07 §2.5/§2.6);⑦ 数据存储表重写 —— 顶部加 v0.8 store 总数说明(10→18+ + SessionDB 不聚合工作流域 store,链 05 §4.0.3 / 02 §4.1.1),AgentStore 补 knowledgeBaseIds 字段,TemplateStore 12→16,KbStore 标 kb_chunks 在独立 knowledge.db(链 06 §2.7),AgentToolStore 行加删除线 + 退役说明,新增 v0.8 工作流域 store 行(列 10 个 store,链 05 §2.2b 矩阵)。**app-flow.md**:① 启动流程 feature hooks 4→7 个(列全 + 标 memory-hooks 删除);② 启动时清理对象 agent-tool→project_wiki 孤儿子树(标 server/index.ts:360-364 v0.8 §8.6 bugfix + 解释原 cleanupOrphans 随 §11.5 退役);③ PostTurnComplete memory extraction→extraction 双提取者;④ Agent 删除流程整段重写(标 afterDelete 级联机制随 AgentToolStore 退役,agent-router.ts:70 注释为证)。源码逐行核对:`node -e "...package.json scripts"`(实际 11 个脚本确认 serve/check:handlers 都不存在)/ `grep "openprd" package.json`(零命中)/ `ls scripts/`(5 文件,无 check-handler-modules)/ `ls src/runtime/hooks/`(7 handler + index,无 memory-hooks)/ `src/runtime/hooks/index.ts:47-59`(registerAllRuntimeHooks 7 个 register 调用 + 第 50 行注释明示 registerMemoryHooks removed)/ `core/hook-types.ts:29-39`(HookEventName 联合 30 个事件名逐行数)/ `src/server/builtin-role-templates.ts:108-119`(BUILTIN_WORKFLOW_ROLES 1 个 zero 确认)/ `template-store.ts:32`(BUILT_IN_TEMPLATES 数组)/ grep `// ── ` template-store.ts(16 个 section header = 16 模板确认)/ `template-store.ts:920-976`(mergeBuiltInTemplates + reconcile stale 确认)/ `grep "typed-ipc" src/`(零命中,确认已删)/ `ls src/main/ipc-proxy.ts`(406 行,registerProxyHandlers + R 表确认)/ `grep "registerCrud" src/`(零命中,确认已删)/ `grep "agentToolStore\|AgentToolStore\|cleanupOrphans" src/`(仅 router/management-service 注释提及 v0.8 §11.5 退役,无实际引用)/ `ls src/server/agent-tool-store.ts`(文件不存在确认)/ `server/index.ts:360-364`(project_wiki 孤儿子树清理 v0.8 §8.6 确认)/ `server/index.ts:475-704`(25 个 app.use 路由挂载点确认)/ `agent-router.ts:70`(no AgentToolStore rows to cascade 注释确认)/ `preload/index.ts`(WindowApi 约 155 个方法确认) | docs/basic/tech-stack.md, docs/basic/backend-structure.md, docs/basic/app-flow.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #16 | 2026-06-27 03:25 | 在 06-knowledge-subsystems.md 新增 §2.7「三套数据库知识系统的对比矩阵」(backlog #38,跨章节一致性 + 知识体系收口)。**问题背景**:06 §1 把记忆主线归为 Wiki tree / KB / Legacy memory 三行,§2 把 project_wiki 讲透,§3/§4 分别讲 KB 与 legacy memory 的当前状态,但**三套并存的存储后端从未放在一起对照** —— 维护者读完 06 仍要在脑子里把"project_wiki 在 sessions.db / kb_chunks 在哪 / memory_* 怎么建"拼起来。更严重的是,核对源码发现**文档叙事里藏了三个不易察觉的事实**:① `kb_chunks` **不在 sessions.db**,而在独立的 `~/.zero-core/knowledge.db`(`KbDB` 自己 `new Database(path)` 开的连接,`kb-db.ts:46-51`),因为每行带 embedding 向量 blob 故意隔离避免拖慢主库 WAL checkpoint;② `memory_nodes` / `memory_subjects` / `memory_edges` / `memory_nodes_fts` **不进 db-migration.ts** —— 它们在 `MemoryNodeStore.init()`(`memory-node-store.ts:138-180`)构造时自建,所以 05 §2.2 的"30 张表"清单里**根本看不到它们**(`grep "memory_nodes" db-migration.ts` 零命中验证);③ 同理 `kb_chunks` 也不在 db-migration(`grep "kb_chunks" db-migration.ts` 零命中),`kb_chunks` 是 `KbDB.initSchema()` 构造时 CREATE。这意味着 05 §2.2 的表计数(30 张)其实是"db-migration 管理的表",**实际 sessions.db 里的表数 > 30**(还要加 memory_* 4 张 + FTS)。改:① **新增 §2.7 物理位置 mermaid 图**:sessions.db / knowledge.db / wiki 磁盘镜像树三个 subgraph,显式画 project_wiki↔磁盘同步虚线、kb_entries↔kb_chunks 跨库 kbId 关联、kb-ingest 双写箭头;② **新增 11 行横向对比矩阵**:11 个维度逐列对比三套系统 —— 当前定位 / 表所在文件 / 建表机制(标 db-migration vs 构造自建两类的区别,**这是关键维护信息**)/ 行规模量级 / 写入触发器(列出 5 个 project_wiki 写入点 + KB 仅 kb-ingest 单点 + memory_* 仅压缩回退)/ 写入约束 / 检索方式(树遍历 vs 向量 cosine vs FTS5 MATCH)/ 谁来读 / 默认 Agent 会话可见? / v0.8 状态 / 是否进 05 §2.2 表;③ **新增"三套并存的历史与取舍"叙事段**:把三套定位为"三代演进留下的地层"(memory_* 最老 → kb_* 中间 → project_wiki 最新主线),讲清为什么不一刀切删前两套(memory_* 压缩回退还在用 / kb_* 基础设施完整只欠产品接入),呼应 §6.2 中期建议;④ **新增"维护者提示"段**:4 条具体改 schema 的入口(改 project_wiki → db-migration;改 kb_chunks → KbDB.initSchema 别去 db-migration;改 memory_* → MemoryNodeStore.init;三套数据不互通 + agents.knowledgeBaseIds vs project_wiki 两套并行 wiki 警告)。源码逐行核对:`kb-db.ts:46-69`(KbDB 构造 + initSchema 确认 knowledge.db 独立文件 + CREATE kb_chunks)/ `kb-store.ts:51`(kb_entries SqliteStore on sessionDB.getDb 确认)/ `memory-node-store.ts:27,94,138-180`(init() 自建 4 表确认 + 注释明确"由 init() 自建")/ `db-migration.ts:672`(project_wiki CREATE 确认 db-migration 管理)/ `grep "kb_chunks" db-migration.ts`(零命中)/ `grep "memory_nodes" db-migration.ts`(零命中)/ `wiki-node-store.ts`(无 cosine/FTS,纯树遍历确认)/ `kb-search.ts:27,59,92`(cosine 相似度确认)/ `memory-node-store.ts:128,281`(FTS MATCH + LIKE 兜底确认)/ `agent-loop.ts:61,116-127`(resolveAnchors + renderSystemAnchors 注入路径确认 wiki 是默认主线)/ `fresh-db-seed.ts:77,342,365,392,399,412,417,424,453` + `project-wiki-router.ts:67,77`(project_wiki 写入点确认) | docs/arch/06-knowledge-subsystems.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #15 | 2026-06-27 02:50 | 在 code-graph 可视化里把 `subagent-delegation.ts` 显式标为死代码 + 顺手修 `test-setup.ts` 缺失的 imports 边(backlog #33 + #34,都是 #6 在 arch md 里建立的"死代码"叙事与可视化数据层不一致的收尾)。**问题背景**:① #6 早已在 02/03/08/12-glossary 四篇 arch md 里把 `subagent-delegation.ts` 标为"零 importer 死代码,删除候选,委派当前走 `subagent-delegator.ts` 的 SubagentDelegator 类",但 `code-graph-data.json`(27k 行)和 `code-graph.html` 内联 DATA(817KB)两个数据源**都**把这两个文件作为平等的合法节点列出,且 `subagent-delegation.ts` 的描述还是"子 agent 委派工厂:blocking / auto-background 模式创建和管理子任务"(完全像活代码),读者打开 code-graph 会被误导;② 同时 `test-setup.ts` 节点标 `exports:[]` / `imports:[]`,但实际文件第 29-30 行 re-export 了 `isTestMode`/`seedTestEnvironment`/`TestSeedResult` 三个符号 from `../core/test-seed.js`(值导入 + type re-export),内联数据完全漏了这条边 —— 这意味着 code-graph 的"被依赖 by"视图里 `src/core/test-seed.ts` 也不会显示 `test-setup.ts` 这一条调用边。**注意**:本次只改这两个节点 + 渲染层(章程硬约束"不要全量重生成 27k 行")。改:① **数据层(JSON)**:`subagent-delegation.ts` 节点加 `"dead": true` + `"supersededBy": "src/runtime/subagent-delegator.ts"`,描述改为"⚠️ 死代码:v0.8 委派重构前的旧实现,src/ 内零 importer(仅注释提及)。当前委派走 subagent-delegator.ts 的 SubagentDelegator 类(被 agent-loop.ts:52 import)";`test-setup.ts` 节点 `exports:[]`→`["isTestMode","seedTestEnvironment","TestSeedResult"]`,`imports:[]`→`["src/core/test-seed.ts"]`,描述改为"src/main 入口侧的测试 facade:透传 isTestMode / seedTestEnvironment / TestSeedResult(实际实现在 src/core/test-seed.ts)";② **数据层(HTML 内联 DATA)**:两个节点同样改动(817KB 单行文件,用 node 脚本做唯一性校验后字符串替换,避免 Edit 工具读不进);③ **渲染层 CSS**:加 `.edge-list li.dead{opacity:0.55}` + `.edge-list li.dead .name{text-decoration:line-through;color:#8b949e}`(灰显+删除线)+ `.dead-badge`(红色 chip 样式)+ `.superseded`/`.superseded code`(红色"已被取代"提示);④ **渲染层 showFileDetail()**:文件详情头部当 `fi.dead===true` 时追加 "DEAD CODE" 红色徽章到 h3,并在描述上方加 `.superseded` div 显示"已被取代 → `<supersededBy>`";⑤ **渲染层 showDirDetail()** 文件列表循环:每行 `li` 当 `fi.dead` 加 `dead` class 触发灰显,并在文件名后追加内联 "DEAD" 小徽章(目录浏览视图也能一眼识别);⑥ **渲染层 renderFileBrowse()**(Calls 面板 File 模式):同样加 `dead` class + 文件名后内联 DEAD 徽章。**验证**:`JSON.parse(standalone JSON)` OK(215 files)/ 内联 DATA brace-match + `JSON.parse` OK(215 files,与外置一致)/ 提取 `<script>` 内容用 `new Function(js)` 解析 OK(JS 语法正确)/ 所有 4 处注入片段 grep 命中。**设计决策**:徽章用红色而非灰色,因为死代码在文档里是"需要读者警觉"的信号,灰显(列表整体 opacity 0.55)+ 红色徽章双重视觉冗余,确保即使读者色盲也能识别(删除线 + 文字标签)。源码逐行核对:`grep -rn "from .*subagent-delegation" src`(零命中,确认死代码)/ `grep -rn "from .*subagent-delegator" src`(命中 `agent-loop.ts:52`)/ `cat src/main/test-setup.ts`(29-30 行确认 re-export)/ `grep "src/core/test-seed.ts" code-graph-data.json`(5249 行,确认是合法节点,边有目标) | docs/visualization/code-graph-data.json, docs/visualization/code-graph.html, docs/.docloop/TRACKER.md | (本次 commit) |
- #14 | 2026-06-27 02:25 | 把 08-cross-cutting.md §2.4 事件分类表与 src/ 实际 `triggerHooks` / `HookRegistry.trigger` 调用逐行核对(backlog #32,跨章节一致性修复,#1 早就修了 02/03 的 hook 清单但 08 §2.4 的事件表一直没动)。**问题背景**:旧 §2.4 表是 v0.7 视角的"按类别分组 + 笼统触发位置",**计数和触发位置都不准**:① 表里 `Setup` 当成 Session 生命周期事件列在"agent-loop.run / .finalizeStream" —— 实际 `grep "Setup" src` 在 `hook-types.ts:31` 定义之外**零命中**(没有任何 triggerHooks 调用);② `TaskCreated` / `TaskCompleted` 标"未装载" —— 实际 `subagent-delegator.ts` 在 `delegateTask` / `delegateTaskBackground` / `runBackground` / `stopTask` 路径里大量触发(199/212/218/269/270/307/319/339/360/373/397/404 共 12 个触发点),这些是 v0.8 委派重构后的**活跃事件**;③ `Notification` 写"触发位置 = TaskRegistry" —— 实际触发点是 `notification-hooks.ts:60`(`HookRegistry.getInstance().trigger("Notification", ...)`),TaskRegistry 只是数据源;④ `TeammateIdle` 标"未装载"是对的,但没说清它和 `TaskCreated`/`TaskCompleted` 的区别(后者是活跃的委派任务事件,前者是占位);⑤ "装载状态" 块写 "✅ 活跃 8 个 / ⚠️ 未装载 23 个" —— **既漏算**(实际活跃 20 个),又把"未装载"等同于"未触发"(真正的幽灵事件只有 10 个);⑥ 8+23=31 与总数 30 都对不齐。**传染点**:§2.6 "可以改进的" 第一条也写 "23 个 hook 定义但未实现" 并举例 `PermissionRequest` / `Elicitation` / `ConfigChange` —— 但 `Elicitation` 实际活跃(AskUser 工具 `tools/ask-user.ts:56,70` 触发),举例本身就错。改:① **§2.4 标题** 加"(类型定义见 `core/hook-types.ts:29-39`)";② **整张表重写** 从"按类别分组 + 笼统触发位置"改为**30 行逐事件矩阵**:每行明确"是否实际触发"(✅/⚠️)+ "实际触发位置(file:line,多个触发点全列)";活跃事件全部标到具体行号(SessionStart=agent-loop.ts:236,296;StopFailure=agent-loop.ts:466;PreToolUse=tool-factory.ts:181 + agent-loop.ts:678;SubagentStop=subagent-delegator.ts 8 个触发点;TaskCreated/TaskCompleted 各 4-6 个触发点;Elicitation/ElicitationResult=ask-user.ts:56,70;Notification=notification-hooks.ts:60 标"不经 triggerHooks 包装";PreCompact/PostCompact=session.ts:156,220,229);幽灵事件(`Setup`/`TeammateIdle`/`PermissionRequest`/`PermissionDenied`/`ConfigChange`/`CwdChanged`/`FileChanged`/`WorktreeCreate`/`WorktreeRemove`/`InstructionsLoaded`)标 "⚠️ 仅定义,零触发";③ **装载状态块** 重写为正确计数:**20 个活跃**(列全)+ **10 个零触发**(列全,解释是参考 Claude Code 27 事件点提前占位的扩展点);④ **新增"过去版本的描述错在哪"更正块**:显式列出旧"活跃 8 / 未装载 23"的两个错误(漏算 12 个本活跃的事件 + 把"未装载"等同"未触发"),让维护者看到旧版判断为何不准;⑤ **新增同名陷阱提示**:`TaskCreated`/`TaskCompleted` 是活跃委派事件,**不是**旧文档暗示的"TeammateIdle 同类未实现占位";⑥ **§2.6 评价** 第一条从"23 个 hook 定义但未实现"改为"10 个 hook 在类型里定义但代码零触发",并把错举例(`Elicitation`)替换为正确的 10 个幽灵事件全列。源码逐行核对:`core/hook-types.ts:29-39`(HookEventName 联合 30 个事件名)/ `grep -oE 'triggerHooks\(\s*"[A-Za-z]+"' src`(提取 19 个 distinct 事件名,Notification 不在内)/ `grep -oE 'HookRegistry.getInstance\(\).trigger\(\s*"[A-Za-z]+"' src`(单命中 `Notification` at `notification-hooks.ts:60`)/ 30 - 20 = 10 个零触发事件名核对(Setup/TeammateIdle/PermissionRequest/PermissionDenied/ConfigChange/CwdChanged/FileChanged/WorktreeCreate/WorktreeRemove/InstructionsLoaded)/ `agent-loop.ts:228,236,251,268,269,296,312,313,466,497,678,705,726,766,798` / `session.ts:156,220,229` / `tool-factory.ts:181,206,220` / `subagent-delegator.ts:140,177,187,199,211,212,217,218,228,269,270,306,307,318,319,338,339,360,373,397,404` / `ask-user.ts:56,70` / `notification-hooks.ts:60` / `durable-hooks.ts:42,60,75,89`(SessionStart/PostToolUse/Stop/StopFailure 4 个 durable handler 确认) / `tool-execution-hooks.ts:38,49,84`(PreToolUse/PostToolUse/PostToolUseFailure 3 个 tool-exec handler 确认) / 02 §3.2 已是 7 个 runtime + 2 个 server handler(无须改动) | docs/arch/08-cross-cutting.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #13 | 2026-06-27 01:50 | 对齐 02 §8.2/§8.3 渲染层组件目录与 07 §5.2/§5.4 页面 section 计数(backlog #43,跨文件一致性修复)。**问题背景**:02 §8.3 列了 11 个 components/ 子目录,但 `ls src/renderer/components/` 实际 13 个 —— 漏了 v0.8 工作流域的 `cron/` / `requirements/` / `wiki/` 3 个目录,且**误列了一个不存在的幽灵目录 `workspace/`**(实际 components/ 下没有 workspace/;01-system-overview.md:257 与 05-persistence.md:18 出现的 `workspace/` 是仓库根的默认工作区目录,是另一回事,不动);`dashboard/` 标"待规划中"但实际已有 `DashboardPage.tsx`。同时 02 §8.3 内文 stale:`agents/` 写"5 个 section"但 AgentEditor 实际 6 个(Basic/Prompt/Permissions/Subagents/Tools/WikiAnchors),且没列全 TemplateGallery/TemplateCard/TemplateDetailModal/GithubImportModal/agent-editor-types.ts;`settings/` 写"9 个 Section"但实际 SettingsPage + 7 个 section 文件(Theme/Proxy/Memory/Guidelines/DeviceContext/Workspace + Provider Card/Editor);`common/` 漏 NotificationToast.tsx。**传染点**:07-renderer-and-ipc.md §5.2 也有同样的"5 个 section + ExposeAsTool"错列(实际无 ExposeAsTool section,有 Subagents+WikiAnchors),§5.4 也写"9 个 section"且列表自身重复(Theme 和 ThemeSettings、DeviceContext 和 DeviceContextSettings 同时列)。file-structure.md #9 已是正确的 13 目录,本次不动它。改:① **02 §8.2** 加一句注:`AppLayout.tsx` 也在 layout/ 下,layout/ 实际 6 个文件(原描述只列 5 个面板,把 AppLayout 隐去);② **02 §8.3 重写目录树**:开篇明示 13 个子目录 + 解释 v0.8 新增 4 个(cron/requirements/skills/wiki)+ 显式说明删 `workspace/` 的理由(避免读者误以为它存在);逐目录补真实文件清单(agents 列 6 个 section + 4 个 template/modal + types;common 补 NotificationToast;cron 单文件 CronDashboard;requirements 列 8 个文件含 Kanban/CoverageJudgement;wiki 列 WikiPage/WikiTree/WikiDetail 链 06;settings 改"8 个文件 = SettingsPage + 7 个 section"列全);③ **07 §5.2** AgentEditor section 计数 5→6 + 列表改正(Basic/Prompt/Tools/Permissions/Subagents/WikiAnchors)+ 链 02 §8.3;④ **07 §5.4** SettingsPage 9→7 个 section + 列表去重 + 链 02 §8.3。源码逐行核对:`ls src/renderer/components/`(13 目录:agents/chat/common/cron/dashboard/kb/layout/mcp/requirements/settings/skills/tools/wiki,**无 workspace**) / `ls src/renderer/components/agents/`(13 文件:AgentsPage/AgentEditor + BasicSection/PromptSection/ToolsSection/PermissionsSection/SubagentsSection/WikiAnchorsSection + TemplateGallery/TemplateCard/TemplateDetailModal/GithubImportModal + agent-editor-types.ts) / `AgentEditor.tsx:33-38`(import 6 个 Section 确认) / `ls src/renderer/components/settings/`(8 文件:SettingsPage + DeviceContext/Guidelines/Memory/Proxy/Theme/Workspace + ProviderCard/ProviderEditor) / `ls src/renderer/components/requirements/`(8 文件) / `ls src/renderer/components/{skills,tools,dashboard}/`(各单文件) / `ls src/renderer/components/common/`(5 文件含 NotificationToast) / grep `workspace/` 确认仅 01:257 / 05:18 / rfc 三处指根目录非 components | docs/arch/02-module-structure.md, docs/arch/07-renderer-and-ipc.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #12 | 2026-06-27 01:25 | 修 05 §4.0.2 + §4.0.3 + §11.2 + file-structure.md 里"SessionDB 聚合 6 个 store"的连锁事实错误(backlog #42,#10 触发时记下的最小改)。**问题背景**:① §4.0.2 标题写"SessionDB 直接聚合的 store(**6 个**,分两批)",正文写"3 个 eager + 2 个 lazy store **+ 1 个全局 wiki store 入口**",但**表格只列 5 行**(3 eager + 2 lazy),第 6 个根本不存在 —— 这是把 `server/index.ts:122` 的 `wikiStoreGlobal = new WikiStore(sessionDB)` **误算进 SessionDB 聚合集**;实际 `WikiStore`(`wiki-node-store.ts:327`)在 `session-db.ts` 里 grep 零命中 —— SessionDB **不**持有 WikiStore 字段、不暴露 getter,只被当 `getDb()` 提供者传进 WikiStore 构造函数。② 这个错计数**传染**到了同文件 §4.0.3 第 561 行("DB 句柄 + 5 张自持表 + **6 个内核 store** 的提供者")和 §11.2 第 810 行("拆分压力只剩会话核心 5 张表 + **6 个内核 store**");③ 还传染到了 `docs/basic/file-structure.md:108`("SessionDB 自持,5 张表 + **6 个聚合 store**")。02-module-structure.md 不受影响(#10 已正确改成 "5 个聚合 store(eager 3 + lazy 2)" + §4.1.1 A 类表)。改:① **§4.0.2 标题** "6 个,分两批" → "5 个,分两批";**正文**删去 "+ 1 个全局 wiki store 入口" 错误尾巴;**新增"⚠️ 更正"块** 显式说明 WikiStore 在 server/index.ts:122 独立 new(链 §4.0.3 + 02 §4.1.1),解释它实例化早(hooks 注册前)与"是否在 SessionDB 内"是两件事;② **§4.0.3 第 561 行** "6 个内核 store" → "5 个内核 store" + 一句"以及 §4.0.2 更正块提到的 WikiStore"把它当 getDb() 提供者;③ **§11.2 第 810 行** "6 个内核 store" → "5 个内核 store";④ **file-structure.md:108** "6 个聚合 store" → "5 个聚合 store" + 顺手把"会话核心聚合根"改成正确的"会话核心 + DB 句柄提供者"(与 §4.0.3 否定"聚合根"叙事一致,链 05 §4.0.2 更正块 / §4.0.3)。源码逐行核对:`grep "WikiStore" src/server/session-db.ts`(零命中,确认 SessionDB 不聚合 WikiStore) / `grep "new WikiStore" src/server`(单命中 `index.ts:122: const wikiStoreGlobal = new WikiStore(sessionDB)`) / `session-db.ts:50-105`(构造函数 + 6 个 getter 实际确认:kvStore/memoryStore/memoryNodeStore eager + extractionCursorStore/telemetryStore lazy,**无 WikiStore 字段**) / `wiki-node-store.ts:327`(export class WikiStore 确认) / 02 §4.1.1 已正确(无须改动) | docs/arch/05-persistence.md, docs/basic/file-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #11 | 2026-06-27 00:50 | 重画 07-renderer-and-ipc.md §3.7 Zustand Store 拓扑 mermaid 图(种子 backlog 第 #29 项,跨章节一致性修复)。**问题背景**:旧 §3.7 图停留在 v0.7:① 只画了一条 `StreamEvent → AppLayout → store` 边,**完全漏了 v0.8 关键的 `data:changed` 第二条事件路径**(各 store 自己用 `data-sync.ts` 订阅,不经 AppLayout);② 漏了 v0.8 工作流域全部 5 个 store(project / requirement / wiki / cron / notification);③ 列了已不存在的 `AgentToolStore` 节点(v0.7 残留,v0.8 工具配置已下沉到服务端 `tool_configs` 表 + `agents.tools` JSON 列,前端工具列表由 `AgentStore.fetchTools()` 从 `agents/{id}/tools` 拉来);④ ChatStore 标"11 字段"过时(实际 8);⑤ 漏了 v0.8 新增的工作流域页面节点(RequirementsPage / WikiPage / CronPage / DashboardPage);⑥ Backend WS 节点没区分两条反向通道(StreamEvent vs data:changed)的语义差异。改:① **顶部加 v0.8 更正块**,显式列出两条事件路径的分工 —— StreamEvent 集中路由 chat 流 + 工作流通知(只更新 chat / interaction / notification / requirement 4 store),data:changed 由各 store **自订阅** 5 个 collection(`agents` / `projects` / `requirements` / `crons` / `project_wiki`),与 AppLayout 完全无关;② **重画图**:把 WS 反向通道拆成 StreamEvent + data:changed 两节点,后者用虚线箭头连到 5 个工作流域 store(显式标"各 store 自订阅,模块副作用"),保留 AppLayout 作为 StreamEvent 唯一订阅者;③ **补 5 个 v0.8 store 节点**(ProjectStore / RequirementStore / WikiStore / CronStore / NotificationStore)+ 4 个工作流域页面节点(DashboardPage / RequirementsPage / WikiPage / CronPage),用浅蓝/浅绿配色与旧 store/页面区分;④ **删过时 AgentToolStore 节点**;⑤ **修正 ChatStore 字段计数** 11→8 + 在"关键观察"列出 8 个实际字段名;⑥ **新增"两条事件路径并行,互不替代"** 关键观察(讲清 StreamEvent 推"事件+增量字段",data:changed 推"完整记录"的本质差异);⑦ **新增"WikiStore 是唯一用 subscribeDataChange(全量 refetch)而非 subscribeListDataChange(增量 patch)的 store"** 观察 —— 因为 wiki 是树形结构,链回 §2.3.1 实际订阅矩阵;⑧ **新增"AppLayout 收到 requirement_notification 时顺手调一次 RequirementStore.fetchRequirements() 是冗余刷新"** 观察(因为 data:changed 也会推 requirements collection)。源码逐行核对:`ls src/renderer/store/`(14 store + data-sync = 15 文件) / `grep "subscribeDataChange\|subscribeListDataChange" src/renderer/store/`(5 处订阅:agent-store:136 / cron-store:127 / project-store:90 / requirement-store:184 / wiki-store:198,确认 wiki 用 subscribeDataChange 其余用 subscribeListDataChange) / `AppLayout.tsx:92-194`(handlers 完整表 14 类事件 + 4 store 更新点:ChatStore / InteractionStore / NotificationStore / RequirementStore) / `AppLayout.tsx:166-175`(requirement_notification handler 确实顺手调 fetchRequirements) / `AppLayout.tsx:196-202`(onAgentEvent 单一订阅确认) / `chat-store.ts:151-158`(8 个 state 字段确认) / `ls src/renderer/components/`(13 目录含 v0.8 新增 cron/requirements/skills/wiki) | docs/arch/07-renderer-and-ipc.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #10 | 2026-06-27 00:25 | 反向同步 05 §4.0.3 的 "SessionDB 不是聚合根" v0.8 叙事到 02-module-structure.md(backlog #36,跨文件一致性修复)。**问题背景**:#8 已在 05 重写 §4 + 新增 §4.0.3 论证 SessionDB **不**聚合 9 个 v0.8 工作流域 store(它们在 `server/index.ts:148-171` 独立 new),但 02 §4 仍停留在 v0.7 "SessionDB = 所有 store 的聚合门面" 叙事 —— ① §4 重头戏表行数过时(wiki-node-store 1090→1698 / agent-service 1010→1200 / db-migration 930→1060 / session-db 850→960 / index.ts 700→904);② §4.1 "11 个 SqliteStore 包装类" 列表完全没覆盖 v0.8 工作流域 9 store + 还列了已删的 agent-tool-store.ts(v0.8 §11.5 退役);③ §4.1 把 session-db.ts 描述为"聚合门面" 暗示它是所有 store 的父聚合根,与 05 §4.0.3 矛盾;④ §4.4 "启动顺序固定: SessionDB → runMigrations → ToolRegistry → Stores → ..." 线性序列漏了 v0.8 关键约束(WikiStore 必须早于 hooks 注册,否则 M5 抽取器拿不到 writer);⑤ §8.4 "10 个 store" 是 v0.7 计数(v0.8 加了 project/requirement/wiki/cron + notification = 14)。改:① **§4 重头戏表** 全部对齐实际行数 + 补 v0.8 角色(db-migration 标 5 阶段链 05 §4.2 / wiki-node-store 标 v0.8 磁盘镜像树链 06 §2.5 / index.ts 标 "手动编排 12+ store" / session-db 标 "会话核心 5 表 + 5 聚合 store" 链 05 §4.0.2)+ 加 "v0.8 关键更正" 引用块 + 架构判断加 "组合根 store 编排应引入 registry";② **§4.1 重写** 从扁平 11 项列表改为 A/B/C 三类归属表(A=SessionDB 直接聚合 5 store eager/lazy 矩阵 / B=旧业务实体 6 store / C=v0.8 工作流域 9+ store 带阶段标注)+ 加 D 已废/特殊类 + ToolConfigStore 同名陷阱说明(文件名 tool-usage-store 含 ToolConfigStore+ToolUsageStore 两类,前者手写 SQL 不基于 SqliteStore)+ **新增 §4.1.1 "store 编排:SessionDB 不是聚合根"** 整节:贴 `index.ts:115-171` 摘录源码 + 解释 v0.8 刻意取舍(3 个 ✅ 优势 + 1 个 ⚠️ 代价 "无 registry / 无自动 DI")+ 交叉链 05 §4.0.3 + ADR-021;③ **§4.4 启动顺序** 从单行线性改为 10 步序列(突出 WikiStore 在 hooks 注册前 new 的关键约束 + CronStore/ProjectStore 工作域 store 互不引用);④ **§8.4 Zustand store 表** 从 10 项扩到 14 项 + data-sync helper 单独行(标"不是 Zustand store")+ v0.8 新增 5 store 加粗 + 各自行数补齐 + 加注解释 "10 个" 是 v0.7 计数。源码逐行核对:`ls src/server/*-store.ts`(22 个文件,减 sqlite-store/key-value/message-store = 19 实际 SqliteStore 子类,但部分文件含多类) / `grep "class " src/server/*-store.ts`(逐个提取 26 个 store 类) / `wc -l` wiki-node-store/session-db/index/db-migration/agent-service/sqlite-store(1698/960/904/1059/1200/393 确认) / `session-db.ts:69-105`(确认 SessionDB 只聚合 KV/Memory/MemoryNode eager + ExtractionCursor/Telemetry lazy = 5 个) / `server/index.ts:113-171`(确认 15+ store 独立 new 序列 + WikiStore 在 hooks 前 new) / `wc -l src/renderer/store/*.ts`(14 store + data-sync = 15 文件) / `ls src/server/agent-tool-store.ts`(确认已删) / `grep "agent-tool-store" src`(仅 router/management-service 注释提及 v0.8 退役,无 import) | docs/arch/02-module-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #9 | 2026-06-26 23:50 | 全文重写 `docs/basic/file-structure.md`(种子 backlog 第二项,定位是"扁平化给新人看的目录速查表",此前所有计数都是 v0.7 旧值,与 src/ 实际严重脱节)。**问题背景**:① "17 个内置工具" —— 实际 ALL_TOOLS 25 entries / 9 categories(#5 已重写 04);② "4 个 feature hook handler:compression/memory/rag/index" —— memory-hooks v0.8 已删,实际 7 个 handler(#1 已修 02/03 的 hook 清单);③ "10 个页面目录" / "10 个 Zustand store" —— 实际 13 个组件目录 / 14 store + 1 helper(v0.8 加了 cron/requirements/skills/wiki 4 域 + project/notification/wiki 4 store);④ "5 个内置 MCP 工具" —— 实际 6 个 mcp-tools 文件(含 cookie-jar);⑤ "16 个 handler 模块"(main 进程)—— v0.8 后 handler 全下沉到 server REST + ipc-proxy 派生,main 只剩 4 个文件;⑥ 完全没提 v0.8 工作流域 9 store(project/requirement/cron/orchestrate/wiki/...);⑦ subagent-delegation.ts 当合法文件列(实际死代码,见 #6)。改:① 顶部定位段补一句"运行时实际 4 个独立 JS 上下文(main/server/renderer/runtime)+ server 是 main fork 的子进程"链到 08 §12;② **核心目录表**从扁平列表改为带链接/职责/关键内容的表格,每层链到 src/ 实际路径;③ **runtime/ 详细结构**拆为顶层文件(逐个补行,标 subagent-delegation 死代码 + subagent-delegator 当前)、hooks(7 个 handler 表 + 注册中心说明 + v0.8 memory-hooks 删除)、tools(25 entries 基础设施 + 按域分组,标 Agent≠AgentRegistry 同名陷阱 + CONDITIONAL_TOOLS 门控)、mcp-tools(6 文件表,标 memory-tools legacy 未注册);④ **server/ 详细结构**重写为按域分组:会话核心(SessionDB 自持 5 表 + 6 聚合 store,链 05 §4.0.3)/ 旧业务实体 / v0.8 工作流域 9 store(标"在 server/index.ts 独立 new,不挂 SessionDB")/ 路由层(20 个 *-router.ts + ROUTE_MAP 派生说明)/ 服务编排(9 个 *-service.ts)/ 基础设施(db-migration 5 阶段链 05 §4.2 + data-change-hub 链 07 §2.3.1 + fresh-db-seed 含 migrateWikiDiskLayout);⑤ **main/ 详细结构**重写为 4 文件(backend-spawn / ipc-proxy 链 07 §2.6 non-2xx reject / test-setup 标 ZERO_CORE_TEST_FIXTURE),显式删"16 handler 模块"叙事;⑥ **renderer/ 详细结构**改 13 组件目录(标 v0.8 新增 4 域)+ 14 store + 1 helper(标 data-sync 是 helper 不是 store);⑦ **命名约定 / 归属规则 / 新增模块该放哪** 三段全部对齐 v0.8:层名表加"进程入口分支文件"、IPC 通道改走 ROUTE_MAP 派生(删手写 ipcMain.handle 旧指引)、持久化实体补"默认在 server/index.ts 独立 new 不挂 SessionDB"、加 feature hook 必须在 hooks/ 注册(强制,不内联 agent-loop.ts)一行;⑧ **维护规则**加一句"计数会随 v0.x 漂移,改之前用 `ls ... | wc -l` 核对,不要照抄本文"。源码逐行核对:`ls src/`(8 顶层)/ `ls src/runtime/`(27 文件)/ `ls src/runtime/tools/*.ts` 减 4 基础设施 = 23 工具文件 + Platform 注入 = 25 entries / `ls src/runtime/hooks/`(7 + index)/ `ls src/runtime/mcp-tools/`(6)/ `ls src/server/`(85 文件)/ `ls src/main/`(4)/ `ls src/renderer/components/`(13)/ `ls src/renderer/store/`(15 = 14 store + data-sync)/ `ls src/shared/`(5)/ `tools/index.ts:70-109`(ALL_TOOLS 25 entries 确认)/ `hooks/index.ts:32-58`(7 个 register*Hooks 确认,memory-hooks 删除确认) | docs/basic/file-structure.md, docs/.docloop/TRACKER.md | (本次 commit) |
- #8 | 2026-06-26 23:25 | 重写 05-persistence.md §4 整章 + §11.2 一行,修两个相互关联的事实错误(backlog #33 + #34,都是源码核对发现的 stale 描述,不是猜测)。**问题背景**:① §4 开篇"当前约 850 行"+"持有 KeyValueStore/MemoryStore/MemoryNodeStore **4 个独立的存储后端**"双重过时 —— v0.8 M5 加了 2 个 lazy store(ExtractionCursorStore/TelemetryStore),且 v0.8 M0~M3 引入的 9 个工作流域 store(ProjectStore/RequirementStore/CronStore/WikiStore/...)在 `server/index.ts:148-171` **独立 new**,根本不挂在 SessionDB 上,所以"4 个后端"既漏了新内核 store 又误把工作流域 store 算进来;② §4.2 "迁移机制 `db-migration.ts:91-223`" 行号过时 —— v0.8 后 `runMigrations` 已扩到 584-1004(420 行),原叙事只覆盖旧 JSON 迁移,**完全漏了 9 张 v0.8 表是怎么被创建的**。改:① **§4 标题**从"业务核心"改为"会话核心(不是业务核心)",明确定位收窄;② **新增 §4.0.1 SessionDB 自持表矩阵**(5 张:sessions/messages/turns/turn_state/tool_executions,各标写入入口);③ **新增 §4.0.2 SessionDB 直接聚合 store 表**(6 个:3 eager 内核 + 2 v0.8 M5 lazy + 说明 lazy 的设计动机"不碰 M5 路径的代码不付初始化成本"+ 两表故意不进 db-migration 的 *_COLUMNS);④ **新增 §4.0.3 关键边界**(v0.8 工作流域 store 在 server/index.ts:159-171 独立 new,把 SessionDB 当 `getDb()` 提供者而非父聚合根;贴 8 行 new 序列源码;解释这个边界是 v0.8 刻意取舍 —— 会话核心不被工作流域写入拖累 + 新增 store 不必改 SessionDB,代价是 server/index.ts 手动编排 12+ store 无 registry);⑤ **新增 §4.0.4 旧叙事更正**段,显式列出此前"4 个后端"说法的两个错误(计数过时 + 误当聚合根);⑥ **§4.2 整段重写**为 5 阶段(阶段 1 列补齐 safeAddColumn 必须先于 SqliteStore 构造 + 列出 agents/providers/sessions/turns 各自补的列 + DROP agent_tools / 阶段 2 v0.8 表 DDL 按 11 步依赖顺序,特别强调 project_wiki 必须先 migrateWikiTableSchema 再 migrateWikiDetailToDisk 否则全局 root 装不进去 + tool_configs 因 PK=tool_name 不进 SqliteStore / 阶段 3 构造 SqliteStore + role_tag 物理列故意补回 / 阶段 4 旧 JSON→SQLite 6 文件含 agent_tools 已删标注 / 阶段 5 KV 6 文件 + Memory)+ 末尾标"v0.8 表无 JSON 前身,阶段 4/5 完全不涉及它们";⑦ **§11.2 session-db.ts 行数** 850→960 + 补一句"拆分压力其实只剩会话核心 5 张表 + 6 个内核 store"(因为工作流域 store 已独立)。源码逐行核对:session-db.ts:37-43,51-108(6 个 store getter 含 2 lazy)/ session-db.ts:960 行 wc 确认 / db-migration.ts:584,1004,289-308,324-416,429-454,474-582(runMigrations 边界 + safeAddColumn/Index + 两个 wiki 迁移子函数) / db-migration.ts:589-847(阶段 1+2 全段) / db-migration.ts:900-1003(阶段 3+4+5 全段) / server/index.ts:148-171(8 个工作流域 store 独立 new,确认不挂 SessionDB) / grep ProjectStore/RequirementStore/CronStore/WikiStore new(全在 server/index.ts,零处在 session-db.ts 内 new) | docs/arch/05-persistence.md | (本次 commit) |
- #7 | 2026-06-26 22:50 | 把 05-persistence.md 从"15 张表 + 重复 ER 图"扩到"30 张表 + 双 ER 图",补齐 v0.8 工作流域 9 张表(此前文档完全没提 projects/requirements/crons/orchestrate_*/project_wiki/wiki_scan_cursors/tool_configs/tool_usage/project_jobs)。**问题背景**:v0.8 M0~M3 落地后 db-migration.ts:653-897 新增了 9 张表,但 05 §2.2 业务实体表还停在 v0.7 的 11 张,§2.13 ER 图既漏新表又有两块完全相同的 mermaid 重复(渲染出两份一样的图)。改:① **§2 标题**:"11 张业务表 + KV + FTS5" → "30 张表:11 业务表 + 9 张 v0.8 工作流域表 + 4 张 SessionDB 内核表 + KV/FTS5/telemetry/cursor";② **§1 目录树** 补 `~/.zero-core/wiki/`(v0.8 P1 §10.1 磁盘镜像树根,project_wiki 正文下沉地),加说明段指 06 §2.5;③ **新增 §2.2b「v0.8 多 Agent 工作流域表」**:14 行矩阵覆盖 projects/project_wiki/wiki_scan_cursors/requirements(×3:主表+history+messages)/task_steps/crons/cron_runs/project_jobs/tool_configs/tool_usage/orchestrate_plans/orchestrate_manifests,每行含 Store 类名+行号+列数+v0.8 阶段(M0/M1/M2/M3/P0)+ 备注;加 ToolConfigStore 手写 SQL 而非 SqliteStore 的说明(PK=tool_name 非 surrogate id);加"与旧业务表边界"段(agents.knowledgeBaseIds vs project_wiki 是两套并行 wiki);④ **§2.13 拆为 2.13a + 2.13b 双 ER 图**:2.13a = 会话/旧业务/记忆(原图,sessions 加 context_project_id 列、turns 加 turn_group 列);2.13b = v0.8 工作流域全新图(projects↔requirements↔task_steps/history/messages、orchestrate_plans↔manifests、crons↔cron_runs、project_wiki self-ref、wiki_scan_cursors、tool_usage/tool_configs/tool_telemetry/extraction_cursors 全部补节点),消除原 duplicate 块;"关键关系"段补 sessions.context_project_id 反向挂 projects、project_wiki project_id 可空、tool_usage≠tool_executions 三条新关系;⑤ **§3** "9 个 Store 的地基" → "18 个 Store 的地基(v0.8 9 张表全复用)";⑥ **§10 性能表** 补 v0.8 索引(idx_turns_session_group / idx_wiki_parent_path / idx_tool_usage_*)+ 备注"v0.8 只加了 step-group 索引,session+seq 全读仍未索引";⑦ **§11.1** 加两条做对了(v0.8 表全 DB-native 省迁移 / safeAddIndex 幂等封装);⑧ **§11.2** 加三条可改进(三套 wiki 并存 / 30 张表导出更紧迫 / tool_usage↔tool_executions 语义重叠应合并 / db-migration.ts 1059 行 DDL 应下沉各 store)。源码逐行核对:db-migration.ts:653-897 全段 9 张 CREATE TABLE + safeAddIndex / project-store.ts:78 / requirement-store.ts:85,93,94 / cron-store.ts:63,176 / orchestrate-store.ts:79,131 / project-job-store.ts:47 / tool-usage-store.ts:51,129(确认 ToolConfigStore 手写 SQL 非 SqliteStore)/ task-step-store.ts:58 / wiki-node-store.ts:327 / wiki-scan-cursor-store.ts:62 / telemetry-store.ts:97 / extraction-cursor-store.ts:77 | docs/arch/05-persistence.md | (本次 commit) |
- #6 | 2026-06-26 22:20 | 修 4 篇 arch md 里关于"子 Agent 委派文件"与"test-seed 边界"的事实错误(都是源码核对发现的 stale 描述,不是猜测)。**问题背景**:`subagent-delegation.ts`(329 行)与 `subagent-delegator.ts`(413 行)是 v0.8 委派重构前后的同义文件,重构后前者零 importer(死代码),后者是 AgentLoop 实际实例化的 `SubagentDelegator` 类;但多份文档把两者关系写反或写糊。改:① **02 §3 表**:把两行从"subagent-delegation=当前/delegator=旧"纠正为"delegator=当前(413 行,被 agent-loop.ts:52 import + Agent 工具调用)/delegation=死代码(零 importer,删除候选)";② **02 §2.1 边界约束**:原"`test-seed.ts` 有 type-only import,属于测试辅助例外"不准确 —— test-seed 既有 5 处 import type(server stores),**也有 1 处值导入** `ensureWikiSkeleton`(from `../server/fresh-db-seed.js`);重写为分层说明(类型层 = 编译时擦除 / 值层 = 真运行时边但生产路径门控不可达)+ 解释为什么不能挪到 tests/(需在 backend 子进程内加载 better-sqlite3 ABI);③ **02 §2 表** `test-seed.ts` 从 `n/a | 测试种子` 改为 `187 | 测试种子(ZERO_CORE_TEST_FIXTURE gated,见 §2.1)`(补真实行数 + 标注门控);④ **03 §1 表**:原"`subagent-delegation.ts`(326):已废,委派统一走 Agent action 工具"两处不准 —— 行数实际 329、且"统一走 Agent 工具"是把"对外工具接口"与"运行时实现"两层混在一起;改为两行:`subagent-delegator.ts`(413,当前)+ `subagent-delegation.ts`(329,死代码);⑤ **03 §3.1 委派模型**:加一段澄清"Agent 工具(协议层)→ SubagentDelegator(运行时层)"两层分工(Agent 工具内部调 `SubagentDelegator.delegateTask()`,后者才是真正建子 loop 跑任务的),并加"子 Agent 上下文隔离 v0.8 关键修复"小节(sessionId=undefined → 不 rebuildFromTurns 父历史 + 不写父 turn 表,消除跨 agent 写竞争);⑥ **08 §2.4 事件分类表**:"子 agent | SubagentStart/Stop | subagent-delegation" 改为 "subagent-delegator(delegateTask/delegateTaskBackground/stopTask 内 triggerHooks)";⑦ **12 术语表**:把单条 "subagent-delegation:子 Agent 委派工厂" 拆为两条 —— `subagent-delegator`(当前类,加完整定义 + hook 列表)+ `subagent-delegation`(历史,死代码,删除候选,显式提示"不要与 delegator 混淆")。源码逐行核对:`grep "from .*subagent-delegation" src`(零命中)/ `grep "from .*subagent-delegator" src`(命中 `agent-loop.ts:52`)/ `grep "test-seed" src`(命中 `main/test-setup.ts:29-30` 值 re-export)/ `test-seed.ts:31-41` 导入分析 / `subagent-delegator.ts:82-236,344-408`(确认 SubagentStart/Stop 在 delegateTask/delegateTaskBackground/stopTask/runBackground 内 triggerHooks)| docs/arch/02-module-structure.md, docs/arch/03-runtime-engine.md, docs/arch/08-cross-cutting.md, docs/arch/12-glossary.md | b31c4c4 |
- #5 | 2026-06-26 21:51 | 重写 04-tools-subsystem.md 与 v0.8 工具子系统对齐(此前文档仍是 ≤ v0.7 视角,21 个工具 + buildAgentTools internal/CLI 二分)。改:① §1 标题与正文从"三层工具分类(built-in/MCP/Agent-as-Tool)"改为"物理来源(2 层) + 语义 category(9 类)",mermaid 图重画,补 v0.8 重要变更说明(原 Agent Tools 第三层取消 + 8 个 zero-admin → 4 action 工具 + Assistant→Platform 改名);② §2 buildTool 行号 92-211 → 163-273;新增 §2.0「buildTool 内置的横切关注点」(PreToolUse 阻断 hook / rateLimiter / 真正 execute / PostToolUse+PostToolUseFailure hook / recordToolUsage 遥测打点 v0.8 P3 §7.7 #4 / truncateResult),这是 v0.8 关键架构提升,此前文档完全没提;③ §2.2 extractInputFields 行号 249-273 → 324-349,补 enum 字段;④ §3 矩阵从 17 行扩到 25 行:新增 CreateRequirement/CreateRequirementWithDoc/Orchestrate/verify(workflow 域)+ Project/AgentRegistry/Cron/Wiki/Platform(management 域),Assistant 改 Platform 并加 RENAMED_TOOLS 说明,Wait 从 task 改为正确的 runtime(核对 src),每行加 CONDITIONAL 门控列,补 v0.8 memory-hooks 删除与同名陷阱(Agent ≠ AgentRegistry)说明;⑤ §5 完全重写——原 buildAgentTools internal/CLI 二分已废,改为 §5.1 Agent 委派工具(agent.ts,action=list/delegate,v0.8 sessionId=undefined 隔离修复跨 agent 写竞争)+ §5.2 AgentRegistry 注册表 CRUD(agent-tool.ts,7 个 action,toolPolicy MERGE 语义,zero 保护,summary 防 context 泛滥);⑥ §6 buildToolsSet 签名从 4 参数改 3 参数(agentTools 参数已删),补"tools map 优先于 autoApprove"的隐含契约;⑦ §8 执行链 mermaid 时序图重画——加 PreToolUse blocked 分支、rateLimiter acquire/release、recordToolUsage 打点(成功/失败两条路径)、truncateResult,删除原 ctx.emit(tool_end) 误述;⑧ §9 安全表删 stale 路径 assistant-tools.ts:29 BLOCKED_FILES(文件已不存在),改写 Platform redactSensitive 输出层、加 PreToolUse 阻断与子 Agent 委派隔离两行、修 evaluateToolCall 行号 core/tool-policy.ts:37;⑨ §12.1 加 buildTool 包装层 / action 合并 / CONDITIONAL 自动收口 / 委派隔离修复 4 项做对了;§12.2 删"internal 共享 DB 写竞争"陈旧评价(v0.8 已修),改为 requiresConfirmation 死字段 / 同名陷阱 / redactSensitive 是输出层补丁 / evaluateToolCall 与 buildToolsSet 双处读 blockedTools 的 drift 风险;⑩ §13 一图总览 21 entries → 25 entries/9 categories,加 buildTool 包装层节点 + RENAMED_TOOLS 节点,标注 agentTools 参数已取消。源码逐行核对:tools/index.ts:70-110,112-126,140-160,157,163-241,195-203 / tools/tool-factory.ts:58-67,163-273,178-235,274,324-349 / tools/agent.ts:1-60,46 / tools/agent-tool.ts:138-260,165,183 / core/tool-registry.ts:65-76,76(RENAMED_TOOLS) / core/tool-policy.ts:37-70 / mcp-tools/platform-tools.ts(全文,redactSensitive) / mcp-tools/fetch-tools.ts:487(WebFetch category) / mcp-tools/sequential-thinking-tools.ts:26-75 | docs/arch/04-tools-subsystem.md | 7f8eac1
- #4 | 2026-06-26 21:20 | 补 08-cross-cutting.md 缺失的 v0.8 后端子进程生命周期 + 全局错误兜底。新增 §12(7 小节 + mermaid flowchart):§12.1 为什么后端是子进程(better-sqlite3 ABI 隔离,dev spawn / packaged fork)、§12.2 启动握手协议(--port=0 + stdout JSON ready 行 + 30s 超时 + stderr 桥接到 logger)、§12.3 自愈(_shuttingDown flag 防竞争 + 重启 fire-and-forget + 端口会变)、§12.4 优雅关闭三段式(stdin shutdown → 5s SIGTERM → 3s SIGKILL)+ Windows SIGTERM/SIGKILL 无语义标注、§12.5 process.on 兜底(只 log 不 exit 的取舍 + stderr→logger 链路)、§12.6 端口暴露与 ipc-proxy 关系、§12.7 架构师评价(4 做对了 + 4 可改进)。同时修正 §2.5 已注册 handler 表(删已废 memory-hooks,补 notification/provider-options/todo-cleanup/extraction/tool-execution hooks,补注册顺序说明)、§2.2/§2.3/§2.6 把 first-writer-wins 改为正确的 "blocked 短路 + 字段 merge last-writer-wins"(与 02/03 #1 一致)、§2.3 时序图重画、§11 安全表 Renderer↔Backend 行从"约 140 代理通道"改为 v0.8 实际(120+ 代理 + 17 LOCAL + 3 INVOKE_NOT_PROXIED + ROUTE_MAP 派生)、§13 清单加两行(后端生命周期 ✅ / 全局错误兜底 ⚠️)。源码逐行核对:backend-spawn.ts:36,56-132,134-164,166-168 / server/index.ts:86-96,110-128 / runtime/hooks/index.ts:32-60 | docs/arch/08-cross-cutting.md | 782c1c3
- #3 | 2026-06-26 20:51 | 对齐 07-renderer-and-ipc.md 与 v0.8 IPC 层实际。改:① §1 store 计数 10→16(含 data-sync helper + project/requirement/wiki/cron/notification stores);② §2.2 R 表从"约 140 项"改为"约 120+ 项,按 26 个域分块注释"+ buildReq 的 params/body/query 三出口说明 + path 占位 encodeURIComponent;③ §2.3.1 data-change-hub 流程图细化(白名单 gate / coalesce / 单独 win.send 防 agent:event 污染)+ 新增"实际订阅矩阵"表(5 collection × 5 store,标 subscribeDataChange vs subscribeListDataChange + 增量策略);④ §2.5 重写"契约例外"——三组集合(LOCAL_CHANNELS 17 项含 orchestrate/pm-handlers / INVOKE_BUT_NOT_PROXIED 3 项 / 退役 agent-as-tool 反向断言)+ 关键澄清 ROUTE_MAP 是测试从 ipc-proxy.ts 源码正则派生而非手写常量 + 标注 search-provider:* 已 v0.8 清理;⑤ 新增 §2.6 v0.8 non-2xx reject(ipc-proxy.ts:324-337,过去静默 resolve 吞 4xx/5xx,现在 throw 带 status+path+excerpt);⑥ §4 AppLayout handler 表 11→14(补 ask_user / requirement_notification / step_failure / verification_failure + message_end 不带 usage 的历史 bug 说明);⑦ §3.2 store 列表补全 v0.8 工作流域 store;⑧ §7 方法计数 150→149 + ROUTE_MAP 派生说明;⑨ §12.2 改写"手写契约"评价 + non-2xx 落地但调用方 try/catch 仍欠缺。源码核对:ipc-proxy.ts:51-274,278-349,355-406 / data-change-hub.ts:31-107 / data-sync.ts:30-70 / rest-routers.test.ts:480-527 / preload/index.ts:87-110,171-174 / AppLayout.tsx:100-205 / store/*.ts 订阅点(agent:136/cron:127/project:90/requirement:184/wiki:198) | docs/arch/07-renderer-and-ipc.md | 15c0d6f
- #2 | 2026-06-26 20:35 | 在 06-knowledge-subsystems.md 新增 §2.5「Wiki 体的磁盘镜像树布局(v0.8 P1 §10.1)」+ §2.6「archivist 增量扫描与摘要懒加载(v0.8 M2)」。§2.5 覆盖:WIKI_DISK_ROOT 隔离根 + isInsideWikiDisk FS 隔离、diskPathFor 路径推导规则表(folder=目录/leaf=文件,global/container/subtree/regular 四类)、leaf→folder promote、改名/reparent 时正文 rename 跟随、migrateWikiDiskLayout 启动一次性幂等迁移、writeNodeDetail/readNodeDetail 永远走推导路径不信任 docPointer、拆库+镜像树的动机。§2.6 覆盖:WikiSkeletonService 命名澄清、buildSkeleton/rescanProjectFull 入口、(archivist,project) git cursor 增量(mermaid 时序图)、ensureSummary 懒加载物化(扫描期零读盘 / 首次 expand 付钱)、唯一调用方 + scope/type 护栏。源码逐行核对:wiki-node-store.ts:197,274,447-469,471-510,584-627,637-740,757-819 / fresh-db-seed.ts:325-337 / wiki-skeleton-service.ts:1-60,160-250,460-540,630-660 / archivist-git.ts:10-15 | docs/arch/06-knowledge-subsystems.md | 5f4dd02
- #1 | 2026-06-26 19:45 | 新增 Hook 系统专节(执行模型 last-writer-wins + blocked 短路 + 错误吞掉、事件→触发点→handler 全景表、PreLLMCall 现查+注入模式、含 mermaid 时序图);修正 02 §3.2 hooks 文件清单(删 memory-hooks、补 notification/provider-options/todo-cleanup/extraction、改注释为实际注册顺序)与 §2 表 hook-registry 描述(改 "first-writer-wins" 为正确的 "last-writer-wins merge + blocked 短路");02 §3.2 加 v0.8 memory-hooks 删除说明。源码核对:hook-registry.ts:78-91 / hook-types.ts:29-39 / hooks/index.ts:47-59 / agent-loop.ts:228,251,296,312,466,497,678,705,726,766 | docs/arch/02-module-structure.md, docs/arch/03-runtime-engine.md | 08117f6

## Open questions(给用户的)
<!-- 拿不准的写这里 -->
- **[new, #24]** **"LOCAL 通道" 计数在多份文档里不一致,需统一口径**。#3/#17/#19 在 07-renderer-and-ipc.md 反复引用 "17 LOCAL + 3 INVOKE_NOT_PROXIED" 这个数字,但 #24 在 11-quality-attributes.md 用 `node` 脚本精确核对 `R 表 141 项` 与 `preload invoke 149 项` 的差集 = **7 个 LOCAL invoke 通道**(window×3 + dialog + webfetch:login + templates:github-preview/import-github),不是 17。差 10 个可能是 07 的 "17" 把 **receive-only event 通道**(agent:event / data:changed / app:ready / tools:changed / session:lifecycle / github-import:progress / github-preview:progress 等 7 个 `ipcRenderer.on` 而非 `invoke`)也算进来了。**待用户拍板**:"LOCAL 通道" 应该只算 invoke(7 个),还是 invoke + receive(7 + 7 ≈ 14)?或者 07 的 "17" 还有别的口径(例如把 INVOKE_BUT_NOT_PROXIED 的 3 个也并了 = 7+3=10,仍不到 17)?这个口径定下来后 07 §2.5 + 11 §8.1 + backend-structure.md 都要对齐同一数字。优先级中(跨文档计数不一致会误导安全审阅者)。
- **[new, #21]** **`MemoryStore` 僵尸是否清理?** #21 源码核对确认 `MemoryStore`(`memory_entities`/`memory_relations`)+ `memory-tools.ts`(memoryReadTool/memoryWriteTool)是**僵尸**(构造但零运行时写入者,唯一消费者 `memory-tools.ts` 自 v0.8 P2 §11.6 从 ALL_TOOLS 取消注册)。完整清理顺序已写进 12-glossary 的 MemoryStore 条目(删 memory-tools.ts → 删 memory-store.ts + 两表 → 删 db-migration.ts:586 的 memory.json 迁移分支 → 删 SessionDB 字段/getter → 删 session-store-interface 接口方法 → 文档侧 05/06/09/10 同步去掉 MemoryStore 行)。**注**:这是动 src/ 的工作,**不归 doc-loop 干**(章程硬约束"不动源码"),只是记在这里作为给用户的工程提案。需用户认可才执行。权衡:留着会增加维护噪音 + 误导读者以为是活跃系统(已被 #21 在 12-glossary 标"僵尸"缓解);删了能省 ~270 行 + 2 张表 + 1 个 SessionDB 字段,但要发 v0.9 破坏性公告(任何依赖 v0.7 `memory.json` 迁移的旧用户升级路径会断)。
- **[new, #7]** `tool_usage`(v0.8 P0 §7.7 #4)与 `tool_executions`(旧 §2.1)语义高度重叠 —— 都是工具调用日志,只是字段集不同(`tool_usage` 有 params+独立 session_id 适合 cron/后台,`tool_executions` 有 input/output_preview 适合 UI 展示)。是否要合并成一张表 + 视图,还是保持两套各司其职?需用户拍板(合并要改 5+ 消费方)。
- **[new, #5]** `meta.requiresConfirmation` 字段在 buildTool/tool-registry/registerRuntimeTools 三处都已贯穿(写进 ToolMeta),但 `agent-loop.ts` 不读它——是死字段。是否要落地一个 PreToolUse handler 把它真正接起来(执行前 AskUser 弹窗)?这会改变"工具自动执行"的默认体感,需要用户拍板默认行为(哪些工具默认 ask、哪些不 ask)。
- **[new, #5]** `Agent`(委派) vs `AgentRegistry`(注册表 CRUD)两个工具名前缀相同,LLM 容易混淆(工具描述里已加一句澄清)。长期是否把 `AgentRegistry` 改名为 `RoleRegistry` / `ManageAgents` 更直观?需权衡 RENAMED_TOOLS 迁移成本 + 用户已有 toolPolicy 配置。
- **[new, #5]** `evaluateToolCall`(core/tool-policy.ts) 与 `buildToolsSet`(tools/index.ts) 双处独立读 `blockedTools`,数据源一致但维护时易 drift。是否要合并成单一真值源?
- **[new, #4]** backend 自愈(`backend-spawn.ts:117-130`)目前无重启计数/退避:连续崩溃会无限静默重启,用户感知不到 backend 死了。是否要加"N 次失败后通知主进程弹用户提示"?
- **[new, #4]** `server/index.ts:86-96` 的 `process.on("uncaughtException", ...)` 只 log 不 exit。这意味着 backend 即使状态已损坏仍继续服务(可能写脏数据)。是否应改为"log + 主动 `process.exit(1)`"让 §12.3 自愈路径接管?权衡是频繁重启可能比脏跑更糟,需要用户拍板默认行为。
- 暂无历史遗留。#1~#5 改进全部基于源码逐行核对,无猜测。
